import{test,expect}from'@playwright/test';test('050-A changes exact-bound room and exports',async({page})=>{await page.goto('/100-builds/050/a');await expect(page.getByRole('heading',{name:'HIGHER LOAD'})).toBeVisible();await page.getByLabel('ROOM').selectOption('QUIET_ROOM');await expect(page.getByRole('status')).toContainText('QUIET ROOM');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT SNAPSHOT'}).click();expect((await dl).suggestedFilename()).toBe('space-effect-snapshot.json')});test('050-A restores and reflows at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/050/a');await page.getByLabel('ROOM').selectOption('QUIET_ROOM');await page.getByRole('button',{name:'RESTORE SCENE'}).click();await expect(page.getByRole('status')).toContainText('OPEN OFFICE');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('050-B advances room state',async({page})=>{await page.goto('/100-builds/050/b');await page.getByRole('button',{name:'CHANGE ROOM OR GOAL'}).click();await expect(page.getByRole('status')).toContainText('QUIET ROOM')});
