import {test,expect} from '@playwright/test';

test.describe('001–006 durable archive backfill',()=>{
 test('program archive declares the website system-of-record policy',async({page})=>{await page.goto('/100-builds/archive');await expect(page.getByRole('heading',{name:'THE WORK DOES NOT LIVE IN CHAT.'})).toBeVisible();await expect(page.getByText(/Every numbered cycle should retain/i)).toBeVisible();for(const id of ['001','002','003','004','005','006'])await expect(page.getByRole('link',{name:new RegExp(`BUILD ${id}`)})).toBeVisible();});
 for(const id of ['001','002','003','004','005','006'])test(`Build ${id} archive preserves all durable record classes`,async({page})=>{await page.goto(`/100-builds/${id}/archive`);await expect(page.getByText('CHAT-INDEPENDENCE RULE')).toBeVisible();for(const label of ['01 / RESEARCH + EVIDENCE','02 / CREATED','03 / ARCHITECTURE + DECISIONS','04 / AUDIT + RELEASE','05 / PLANS + OPEN THREADS','06 / MEDIA','07 / CANONICAL SOURCE INVENTORY'])await expect(page.getByText(label,{exact:true})).toBeVisible();await expect(page.locator('code').first()).toBeVisible();});
 test('005 and 006 render as real lab rooms with working A/B/archive routes',async({page})=>{for(const id of ['005','006']){await page.goto(`/100-builds/${id}`);await expect(page.getByText(new RegExp(`BUILD ${id} / 100 · IN THE LAB`))).toBeVisible();await expect(page.getByRole('link',{name:/OPEN THE TOOL/})).toHaveAttribute('href',`/100-builds/${id}/a`);await expect(page.getByRole('link',{name:/ENTER THE VISUAL BUILD/})).toHaveAttribute('href',`/100-builds/${id}/b`);await expect(page.getByRole('link',{name:/Research \+ creation \+ decisions \+ audits \+ plans/})).toHaveAttribute('href',`/100-builds/${id}/archive`);}});
 for(const route of ['/100-builds/archive','/100-builds/001/archive','/100-builds/006/archive'])test(`${route} remains usable at 320px`,async({page})=>{await page.setViewportSize({width:320,height:900});await page.goto(route);const g=await page.evaluate(()=>({client:document.documentElement.clientWidth,scroll:document.documentElement.scrollWidth}));expect(g.scroll-g.client,`${route}: ${JSON.stringify(g)}`).toBeLessThanOrEqual(1);});
});
