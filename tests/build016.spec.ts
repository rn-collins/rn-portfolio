import {test,expect} from '@playwright/test';test.describe('Build 016 legal workflow mapping',()=>{test('default workflow is mapped and accountable',async({page})=>{await page.goto('/100-builds/016/a');await expect(page.getByRole('heading',{name:'MAPPED AND ACCOUNTABLE'})).toBeVisible();await expect(page.getByText(/100% mapped/)).toBeVisible()});test('missing acceptance exposes handoff gaps',async({page})=>{await page.goto('/100-builds/016/a');await page.getByRole('button',{name:'REMOVE DECISION ACCEPTANCE'}).click();await expect(page.getByRole('heading',{name:'HANDOFF GAPS'})).toBeVisible();await expect(page.getByText(/H04: define receiver, acceptance, record/)).toBeVisible()});test('reset exposes undefined workflow',async({page})=>{await page.goto('/100-builds/016/a');await page.getByRole('button',{name:'RESET / START EMPTY'}).click();await expect(page.getByRole('heading',{name:'WORKFLOW UNDEFINED'})).toBeVisible()});test('exports workflow map',async({page})=>{await page.goto('/100-builds/016/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT WORKFLOW MAP'}).click();expect((await dl).suggestedFilename()).toBe('legal-workflow-map.json')});test('lifecycle advances to 017',async({page})=>{await page.goto('/100-builds/016');await expect(page.getByText('BUILD 016 / 100 · IN THE LAB')).toBeVisible();await page.goto('/100-builds/017');await expect(page.getByText('BUILD 017 / 100 · COMING NEXT')).toBeVisible()});test('fits 320 CSS pixels',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/016/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)})});test('paired visual reveals the record layer',async({page})=>{await page.goto('/100-builds/016/b');await page.getByRole('button',{name:/05 RECORD/}).click();await expect(page.getByRole('heading',{name:'RECORD'})).toBeVisible();await expect(page.getByText(/Instruction · review · decision/)).toBeVisible()});
