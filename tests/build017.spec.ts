import {test,expect} from '@playwright/test';test.describe('Build 017 regulated-market handoffs',()=>{test('default chain keeps accountability intact',async({page})=>{await page.goto('/100-builds/017/a');await expect(page.getByRole('heading',{name:'ACCOUNTABILITY INTACT'})).toBeVisible();await expect(page.getByText(/100% controlled/)).toBeVisible()});test('missing data-transfer controls expose responsibility gaps',async({page})=>{await page.goto('/100-builds/017/a');await page.getByRole('button',{name:'REMOVE DATA TRANSFER CONTROLS'}).click();await expect(page.getByRole('heading',{name:'RESPONSIBILITY GAPS'})).toBeVisible();await expect(page.getByText(/X02: define permission basis, incident duty, recourse/)).toBeVisible()});test('reset exposes undefined chain',async({page})=>{await page.goto('/100-builds/017/a');await page.getByRole('button',{name:'RESET / START EMPTY'}).click();await expect(page.getByRole('heading',{name:'CHAIN UNDEFINED'})).toBeVisible()});test('exports handoff map',async({page})=>{await page.goto('/100-builds/017/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT HANDOFF MAP'}).click();expect((await dl).suggestedFilename()).toBe('regulated-market-handoff-map.json')});test('lifecycle advances to 018',async({page})=>{await page.goto('/100-builds/017');await expect(page.getByText('BUILD 017 / 100 · IN THE LAB')).toBeVisible();await page.goto('/100-builds/018');await expect(page.getByText('BUILD 018 / 100 · COMING NEXT')).toBeVisible()});test('fits 320 CSS pixels',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/017/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)})});test('paired visual reveals permission gap',async({page})=>{await page.goto('/100-builds/017/b');await page.getByRole('button',{name:/PERMISSION/}).click();await expect(page.getByRole('heading',{name:'PERMISSION'})).toBeVisible();await expect(page.getByText(/What authorizes this exact transfer and use/)).toBeVisible()});
