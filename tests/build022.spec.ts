import {test,expect} from '@playwright/test';
test.describe('Build 022 human consequence map',()=>{
 test('discloses representation gaps and lets each affected node be repaired',async({page})=>{await page.goto('/100-builds/022/a');await expect(page.getByRole('heading',{name:'REPRESENTATION GAPS'})).toBeVisible();await page.getByRole('button',{name:/P-01/}).click();await page.getByLabel('Affected-person knowledge is represented').check();await expect(page.getByRole('status')).toContainText('REPRESENTATION GAPS')});
 test('keeps distinct people records separate',async({page})=>{await page.goto('/100-builds/022/a');await page.getByRole('button',{name:/P-02/}).click();await expect(page.getByRole('heading',{name:'Household members'})).toBeVisible();await page.getByLabel('Meaningful notice exists').check();await page.getByRole('button',{name:/P-01/}).click();await expect(page.getByLabel('Meaningful notice exists')).toBeChecked()});
 test('exports a client-local portable record',async({page})=>{await page.goto('/100-builds/022/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT MAP'}).click();expect((await dl).suggestedFilename()).toBe('human-consequence-map.json')});
 test('fits 320 CSS pixels',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/022/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});
 test('paired visual changes assumption accessibly',async({page})=>{await page.goto('/100-builds/022/b');await page.getByRole('button',{name:'CHANGE THE ASSUMPTION'}).click();await expect(page.getByRole('status')).toContainText('AUTOMATED ACTION')});
});
