import {test,expect} from '@playwright/test';test.describe('Build 030 inclusive repair lab',()=>{test('maps selected conditions to unresolved repairs',async({page})=>{await page.goto('/100-builds/030/a');await expect(page.getByRole('heading',{name:'EXCLUSION RISKS'})).toBeVisible();await expect(page.getByText(/keyboardOnly: enable visibleFocus/)).toBeVisible()});test('applies the complete repair set',async({page})=>{await page.goto('/100-builds/030/a');await page.getByRole('button',{name:'APPLY REPAIR SET'}).click();await expect(page.getByRole('heading',{name:'REPAIRED'})).toBeVisible();await expect(page.getByText(/All selected conditions/)).toBeVisible()});test('exports locally',async({page})=>{await page.goto('/100-builds/030/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT REPAIR RECORD'}).click();expect((await dl).suggestedFilename()).toBe('inclusive-repair-record.json')});test('fits 320px',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/030/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});test('B transforms accessibly',async({page})=>{await page.goto('/100-builds/030/b');await page.getByRole('button',{name:'TRANSFORM THE INTERFACE'}).click();await expect(page.getByRole('status')).toContainText('APPLY CONDITIONS')})});
