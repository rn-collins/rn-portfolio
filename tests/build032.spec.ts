import {test,expect} from '@playwright/test';test('032-A blocks and then completes synthetic evidence',async({page})=>{await page.goto('/100-builds/032/a');await expect(page.getByRole('heading',{name:'Consequential AI Release Readiness Harness'})).toBeVisible();await expect(page.getByRole('heading',{name:'BLOCKED'})).toBeVisible();await page.getByRole('button',{name:'LOAD COMPLETE EVIDENCE FIXTURE'}).click();await expect(page.getByRole('heading',{name:'EVIDENCE-READY'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT RELEASE RECORD'}).click();expect((await dl).suggestedFilename()).toBe('consequential-release-readiness-record.json')});test('032-A has no horizontal overflow at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/032/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('032-B physically clears blockers accessibly',async({page})=>{await page.goto('/100-builds/032/b');const status=page.getByRole('status');await expect(status).toContainText('RELEASE BLOCKED');for(let i=0;i<5;i++)await page.getByRole('button',{name:/CLEAR .* GATE/}).click();await expect(status).toContainText('EVIDENCE PATH OPEN')});
