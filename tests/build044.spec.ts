import{test,expect}from'@playwright/test';test('044-A invalidates changed context',async({page})=>{await page.goto('/100-builds/044/a');await expect(page.getByRole('heading',{name:'ASSEMBLY VALID'})).toBeVisible();await page.getByRole('button',{name:'CHANGE JURISDICTION'}).click();await expect(page.getByRole('heading',{name:'INVALIDATED'})).toBeVisible();await page.getByRole('button',{name:'REVALIDATE COMPONENTS'}).click();await expect(page.getByRole('heading',{name:'ASSEMBLY VALID'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT ASSEMBLY'}).click();expect((await dl).suggestedFilename()).toBe('regulated-component-assembly.json')});test('044-A fits 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/044/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('044-B exposes context change',async({page})=>{await page.goto('/100-builds/044/b');await page.getByRole('button',{name:'CHANGE CONTEXT'}).click();await expect(page.getByRole('status')).toContainText('CONTEXT CHANGE')});
