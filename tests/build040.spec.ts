import {test,expect} from '@playwright/test';test('040-A exercises boundary and exports',async({page})=>{await page.goto('/100-builds/040/a');await expect(page.getByRole('heading',{name:'CONTINUE'})).toBeVisible();await page.getByRole('button',{name:'high Risk'}).click();await expect(page.getByRole('heading',{name:'ESCALATE'})).toBeVisible();await page.getByRole('button',{name:'no Recourse'}).click();await expect(page.getByRole('heading',{name:'ABSTAIN'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT BOUNDARY RECORD'}).click();expect((await dl).suggestedFilename()).toBe('stop-escalate-decision-record.json')});test('040-A no overflow',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/040/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('040-B changes accessibly',async({page})=>{await page.goto('/100-builds/040/b');await page.getByRole('button',{name:'CHANGE SIGNALS'}).click();await expect(page.getByRole('status')).toContainText('ASK')});
