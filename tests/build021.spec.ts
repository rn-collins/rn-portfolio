import {test,expect} from '@playwright/test';test.describe('Build 021 vendor claim x-ray',()=>{test('preserves conditional state and closes exact verification paths',async({page})=>{await page.goto('/100-builds/021/a');await expect(page.getByRole('heading',{name:'CONDITIONAL'})).toBeVisible();await page.getByLabel('Independent verification exists').check();await page.getByLabel('Commitment is enforceable').check();await expect(page.getByRole('heading',{name:'VERIFIED FOR THIS SCOPE'})).toBeVisible()});test('changes selected claim without conflating records',async({page})=>{await page.goto('/100-builds/021/a');await page.getByRole('button',{name:/CL-03/}).click();await expect(page.getByText('SELECTED CLAIM / CL-03')).toBeVisible();await expect(page.getByRole('heading',{name:'UNRESOLVED'})).toBeVisible()});test('exports client-local x-ray',async({page})=>{await page.goto('/100-builds/021/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT X-RAY'}).click();expect((await dl).suggestedFilename()).toBe('ai-vendor-claim-xray.json')});test('fits 320 CSS pixels',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/021/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});test('paired visual advances accessibly',async({page})=>{await page.goto('/100-builds/021/b');await page.getByRole('button',{name:'ADVANCE THE X-RAY'}).click();await expect(page.getByRole('status')).toContainText('IDENTITY')})});