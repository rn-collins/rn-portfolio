import {test,expect} from '@playwright/test';test('039-A repairs and exports trace',async({page})=>{await page.goto('/100-builds/039/a');await expect(page.getByRole('heading',{name:'CONTROL GAPS'})).toBeVisible();await page.getByRole('button',{name:'REPLAY WITH VERIFIED EVIDENCE'}).click();await expect(page.getByRole('heading',{name:'INSPECTABLE FIXTURE'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT TRACE RECORD'}).click();expect((await dl).suggestedFilename()).toBe('agent-work-trace-record.json')});test('039-A no overflow',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/039/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('039-B replays accessibly',async({page})=>{await page.goto('/100-builds/039/b');await page.getByRole('button',{name:'BRANCH AND REPLAY'}).click();await expect(page.getByRole('status')).toContainText('BRANCH THE TRACE')});
