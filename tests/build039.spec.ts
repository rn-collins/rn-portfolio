import {test,expect} from '@playwright/test';test('039-A blocks missing evidence, repairs, and exports bounded trace',async({page})=>{await page.goto('/100-builds/039/a');await expect(page.getByRole('heading',{name:'TRACE BLOCKED'})).toBeVisible();await expect(page.getByText(/hidden reasoning/)).toBeVisible();await page.getByRole('button',{name:'REPAIR RECORDED EVIDENCE'}).click();await expect(page.getByRole('heading',{name:'BOUNDED SYNTHETIC TRACE COMPLETE'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT BOUNDED TRACE'}).click();expect((await dl).suggestedFilename()).toBe('agent-work-trace-record.json')});test('039-A keyboard and 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/039/a');await page.keyboard.press('Tab');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('039-B replays accessibly',async({page})=>{await page.goto('/100-builds/039/b');await page.getByRole('button',{name:'BRANCH AND REPLAY'}).click();await expect(page.getByRole('status')).toContainText('BRANCH THE TRACE')});