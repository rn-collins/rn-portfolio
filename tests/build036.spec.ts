import {test,expect} from '@playwright/test';test('036-A cuts dependencies, hardens fallback and exports',async({page})=>{await page.goto('/100-builds/036/a');await expect(page.getByRole('heading',{name:'Resilience Mode / Offline Survival Test'})).toBeVisible();await page.getByRole('button',{name:'SEVER ALL DEPENDENCIES'}).click();await expect(page.getByRole('heading',{name:'CRITICAL LOSS'})).toBeVisible();await page.getByRole('button',{name:'ENABLE QUEUED FALLBACKS'}).click();await expect(page.getByRole('heading',{name:'SURVIVES'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT SURVIVAL RECORD'}).click();expect((await dl).suggestedFilename()).toBe('offline-survival-record.json')});test('036-A has no overflow at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/036/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('036-B changes dependency state accessibly',async({page})=>{await page.goto('/100-builds/036/b');const status=page.getByRole('status');await expect(status).toContainText('EVERYTHING CONNECTED');await page.getByRole('button',{name:'CHANGE NETWORK STATE'}).click();await expect(status).toContainText('DEPENDENCIES SEVERED')});
