import {test,expect} from '@playwright/test';test.describe('Build 020 decision evidence ledger',()=>{test('reconstructs and exposes evidence gaps',async({page})=>{await page.goto('/100-builds/020/a');await expect(page.getByRole('heading',{name:'3 / 3 evidence records linked'})).toBeVisible();await page.getByLabel(/Severity assessment/).uncheck();await page.getByLabel(/Escalation acknowledgement/).uncheck();await expect(page.getByText('EVIDENCE GAP')).toBeVisible()});test('appends correction without erasure',async({page})=>{await page.goto('/100-builds/020/a');await page.getByRole('button',{name:'APPEND CORRECTION'}).click();await expect(page.getByText('Correction appended; original retained')).toBeVisible()});test('exports local ledger',async({page})=>{await page.goto('/100-builds/020/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT LEDGER'}).click();expect((await dl).suggestedFilename()).toBe('decision-evidence-ledger.json')});test('fits 320 CSS pixels',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/020/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});test('paired visual advances',async({page})=>{await page.goto('/100-builds/020/b');await page.getByRole('button',{name:'ADVANCE THE RECORD'}).click();await expect(page.getByText('SELECTED STAGE / ACTOR + AUTHORITY')).toBeVisible()})});