import {test,expect} from '@playwright/test';test.describe('Build 025 implementation readiness',()=>{test('does not bypass inherited vendor verification',async({page})=>{await page.goto('/100-builds/025/a');await expect(page.getByRole('heading',{name:'NOT READY'})).toBeVisible();await expect(page.getByText(/vendor claim verification path/)).toBeVisible()});test('reaches only a bounded pilot after evidence',async({page})=>{await page.goto('/100-builds/025/a');await page.getByRole('button',{name:'ADD MISSING CONTROL EVIDENCE'}).click();await expect(page.getByRole('heading',{name:'READY FOR BOUNDED PILOT'})).toBeVisible()});test('exports locally',async({page})=>{await page.goto('/100-builds/025/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT READINESS RECORD'}).click();expect((await dl).suggestedFilename()).toBe('ai-implementation-readiness.json')});test('fits 320px',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/025/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});test('B advances accessibly',async({page})=>{await page.goto('/100-builds/025/b');await page.getByRole('button',{name:'REVEAL THE NEXT LAYER'}).click();await expect(page.getByRole('status')).toContainText('OPEN THE X-RAY')})});