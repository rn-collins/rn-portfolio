import {test,expect} from '@playwright/test';
test.describe('Build 023 consent rights manager',()=>{test('records withdrawal without erasing the prior state',async({page})=>{await page.goto('/100-builds/023/a');await expect(page.getByRole('heading',{name:'RIGHTS PATHS READY'})).toBeVisible();await page.getByRole('button',{name:'WITHDRAW THIS PURPOSE'}).click();await expect(page.getByText(/EVT-001 · WITHDRAWN/)).toBeVisible();await expect(page.getByRole('status')).toContainText('1 active · 1 withheld or withdrawn · 1 valid session event')});test('keeps purposes distinct',async({page})=>{await page.goto('/100-builds/023/a');await page.getByRole('button',{name:/PUR-02/}).click();await expect(page.getByRole('heading',{name:'Optional service-improvement research'})).toBeVisible();await expect(page.getByRole('button',{name:'GRANT THIS PURPOSE'})).toBeVisible()});test('preserves a grant-withdrawal chain',async({page})=>{await page.goto('/100-builds/023/a');await page.getByRole('button',{name:/PUR-02/}).click();await page.getByRole('button',{name:'GRANT THIS PURPOSE'}).click();await page.getByRole('button',{name:'WITHDRAW THIS PURPOSE'}).click();await expect(page.getByRole('status')).toContainText('2 valid session event');await expect(page.getByText(/EVT-002 · WITHDRAWN/)).toBeVisible()});test('exports locally',async({page})=>{await page.goto('/100-builds/023/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT RIGHTS RECORD'}).click();expect((await dl).suggestedFilename()).toBe('consent-rights-record.json')});test('fits 320px',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/023/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});test('B advances accessibly',async({page})=>{await page.goto('/100-builds/023/b');await page.getByRole('button',{name:'ADVANCE THE RIGHTS RECORD'}).click();await expect(page.getByRole('status')).toContainText('PURPOSES SEPARATED')})});