import {test,expect} from '@playwright/test';test.describe('Build 028 transparent evidence search',()=>{test('shows the ranking rationale',async({page})=>{await page.goto('/100-builds/028/a');await expect(page.getByRole('heading',{name:'RESULTS'})).toBeVisible();await page.getByText('Why this ranked here').first().click();await expect(page.getByText(/Primary-source record receives/).first()).toBeVisible()});test('an empty query does not return the corpus',async({page})=>{await page.goto('/100-builds/028/a');await page.getByLabel('Question or search terms').fill('');await expect(page.getByRole('heading',{name:'QUERY NEEDED'})).toBeVisible();await expect(page.locator('ol > li')).toHaveCount(0)});test('facets change the result set',async({page})=>{await page.goto('/100-builds/028/a');await page.getByLabel('Topic facet').selectOption('privacy');await expect(page.locator('ol > li')).toHaveCount(0);await page.getByLabel('Question or search terms').fill('privacy');await expect(page.getByText('Data rights operating note')).toBeVisible()});test('exports locally',async({page})=>{await page.goto('/100-builds/028/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT SEARCH TRACE'}).click();expect((await dl).suggestedFilename()).toBe('transparent-search-trace.json')});test('fits 320px',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/028/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});test('B reranks accessibly',async({page})=>{await page.goto('/100-builds/028/b');await page.getByRole('button',{name:'PRIVACY'}).click();await expect(page.getByRole('status')).toContainText('PRIVACY')})});
