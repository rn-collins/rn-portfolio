import {test,expect} from '@playwright/test';test.describe('Build 018 institutional friction',()=>{test('default path exposes friction hotspots',async({page})=>{await page.goto('/100-builds/018/a');await expect(page.getByRole('heading',{name:'FRICTION HOTSPOTS'})).toBeVisible();await expect(page.getByText(/BOTTLENECK/).first()).toBeVisible();await expect(page.getByText(/REPEATED LOOP/).first()).toBeVisible();await expect(page.getByText(/DUPLICATE DEMAND/).first()).toBeVisible()});test('bounded repair creates an accessible path',async({page})=>{await page.goto('/100-builds/018/a');await page.getByRole('button',{name:'SIMULATE BOUNDED REPAIR'}).click();await expect(page.getByRole('heading',{name:'ACCESSIBLE PATH'})).toBeVisible()});test('reset exposes undefined process',async({page})=>{await page.goto('/100-builds/018/a');await page.getByRole('button',{name:'RESET / START EMPTY'}).click();await expect(page.getByRole('heading',{name:'PROCESS UNDEFINED'})).toBeVisible()});test('exports friction map',async({page})=>{await page.goto('/100-builds/018/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT FRICTION MAP'}).click();expect((await dl).suggestedFilename()).toBe('institutional-friction-map.json')});test('lifecycle advances to 019',async({page})=>{await page.goto('/100-builds/018');await expect(page.getByText('BUILD 018 / 100 · IN THE LAB')).toBeVisible();await page.goto('/100-builds/019');await expect(page.getByText('BUILD 019 / 100 · COMING NEXT')).toBeVisible()});test('fits 320 CSS pixels',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/018/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)})});test('paired visual simulates repair',async({page})=>{await page.goto('/100-builds/018/b');await page.getByRole('button',{name:'SIMULATE REPAIR'}).click();await expect(page.getByText('SELECTED STATE / DECIDE')).toBeVisible();await expect(page.getByRole('heading',{name:'ONE NOTICE'})).toBeVisible()});
