import {test,expect} from '@playwright/test';
test('031-A minimizes telemetry and exports evidence',async({page})=>{await page.goto('/100-builds/031/a');await expect(page.getByRole('heading',{name:'Privacy-Conscious Product Behavior X-Ray'})).toBeVisible();await expect(page.getByRole('heading',{name:'OVER-COLLECTING'})).toBeVisible();await page.getByRole('button',{name:'APPLY MINIMUM SET'}).click();await expect(page.getByRole('heading',{name:'MINIMIZED'})).toBeVisible();const download=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT MEASUREMENT RECORD'}).click();expect((await download).suggestedFilename()).toBe('privacy-measurement-record.json')});
test('031-A has no horizontal overflow at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/031/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});
test('031-B reveals the minimum useful signal',async({page})=>{await page.goto('/100-builds/031/b');const status=page.getByRole('status');await expect(status).toContainText('EVERYTHING IS VISIBLE');await page.getByRole('button',{name:'REDUCE THE TELEMETRY'}).click();await page.getByRole('button',{name:'REDUCE THE TELEMETRY'}).click();await expect(status).toContainText('MINIMUM USEFUL SIGNAL')});
