import{test,expect}from'@playwright/test';test('043-A gates unsupervised expansion',async({page})=>{await page.goto('/100-builds/043/a');await expect(page.getByRole('heading',{name:'TEAM READY'})).toBeVisible();await page.getByRole('button',{name:'ADD DRAFTER'}).click();await expect(page.getByRole('heading',{name:'TEAM GAPS'})).toBeVisible();await page.getByRole('button',{name:'ASSIGN HUMAN SUPERVISOR'}).click();await expect(page.getByRole('heading',{name:'TEAM READY'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT TEAM TOPOLOGY'}).click();expect((await dl).suggestedFilename()).toBe('ai-team-topology.json')});test('043-A fits 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/043/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('043-B exposes topology state',async({page})=>{await page.goto('/100-builds/043/b');await page.getByRole('button',{name:'CHANGE TOPOLOGY'}).click();await expect(page.getByRole('status')).toContainText('HANDOFF')});
