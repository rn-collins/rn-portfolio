import{test,expect}from'@playwright/test';test('049-A changes context and exports a private snapshot',async({page})=>{await page.goto('/100-builds/049/a');await expect(page.getByRole('heading',{name:'MIXED'})).toBeVisible();await page.getByLabel('SETTING').selectOption('TRAVEL');await expect(page.getByRole('status')).toContainText('TRAVEL');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT PRIVATE SNAPSHOT'}).click();expect((await dl).suggestedFilename()).toBe('private-context-profile.json')});test('049-A restores and reflows at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/049/a');await page.getByLabel('GOAL').selectOption('REST');await page.getByRole('button',{name:'RESTORE PROFILE'}).click();await expect(page.getByRole('status')).toContainText('FOCUS');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('049-B advances synthetic context',async({page})=>{await page.goto('/100-builds/049/b');await page.getByRole('button',{name:'CHANGE CONTEXT'}).click();await expect(page.getByRole('status')).toContainText('HOME')});
