import{test,expect}from'@playwright/test';test('048-A propagates procedural change and exports',async({page})=>{await page.goto('/100-builds/048/a');await expect(page.getByRole('heading',{name:'BURDENSOME'})).toBeVisible();await page.getByRole('button',{name:'APPLY PROCEDURAL CHANGE'}).click();await expect(page.getByRole('status')).toContainText('Synthetic Rule v2');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT COMPARISON'}).click();expect((await dl).suggestedFilename()).toBe('rule-lived-experience.json')});test('048-A restores and fits 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/048/a');await page.getByRole('button',{name:'APPLY PROCEDURAL CHANGE'}).click();await page.getByRole('button',{name:'RESTORE ORIGINAL RULE'}).click();await expect(page.getByRole('status')).toContainText('Synthetic Rule v1');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('048-B advances procedure',async({page})=>{await page.goto('/100-builds/048/b');await page.getByRole('button',{name:'CHANGE PROCEDURE'}).click();await expect(page.getByRole('status')).toContainText('THE PATH')});
