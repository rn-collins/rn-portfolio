import{test,expect}from'@playwright/test';test('047-A exact transfer and atomic export',async({page})=>{await page.goto('/100-builds/047/a');await expect(page.getByRole('heading',{name:'FITS'})).toBeVisible();await page.getByRole('button',{name:'MOVE INTERVENTION'}).click();await expect(page.getByRole('heading',{name:'DOES NOT FIT'})).toBeVisible();await expect(page.getByRole('status')).toContainText('BUDGET · WORKFORCE · CONNECTIVITY · LANGUAGE · DEPENDENCY · MOBILITY');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT TEST'}).click();const download=await dl;expect(download.suggestedFilename()).toBe('place-transfer-test.json');const body=JSON.parse(await (await import('node:fs/promises')).readFile(await download.path() as string,'utf8'));expect(body.version).toBe('047.2.0');expect(body.place.id).toBe('B');expect(body.result.status).toBe('DOES NOT FIT');expect(body.replay.mode).toBe('state-only')});test('047-A restores and fits 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/047/a');await page.getByRole('button',{name:'MOVE INTERVENTION'}).click();await page.getByRole('button',{name:'RESTORE ORIGIN'}).click();await expect(page.getByRole('heading',{name:'FITS'})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('047-B advances transfer',async({page})=>{await page.goto('/100-builds/047/b');await page.getByRole('button',{name:'MOVE INTERVENTION'}).click();await expect(page.getByRole('status')).toContainText('TRANSFER')});
