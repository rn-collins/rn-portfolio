import{test,expect}from'@playwright/test';test('047-A recomputes place transfer and exports',async({page})=>{await page.goto('/100-builds/047/a');await expect(page.getByRole('heading',{name:'FITS'})).toBeVisible();await page.getByRole('button',{name:'MOVE INTERVENTION'}).click();await expect(page.getByRole('heading',{name:'DOES NOT FIT'})).toBeVisible();await expect(page.getByRole('status')).toContainText('CONNECTIVITY');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT TEST'}).click();expect((await dl).suggestedFilename()).toBe('place-transfer-test.json')});test('047-A fits 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/047/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('047-B advances transfer',async({page})=>{await page.goto('/100-builds/047/b');await page.getByRole('button',{name:'MOVE INTERVENTION'}).click();await expect(page.getByRole('status')).toContainText('TRANSFER')});
