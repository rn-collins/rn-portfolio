import {test,expect} from '@playwright/test';test.describe('Build 027 creator ownership stack',()=>{test('exposes the dependent distribution layer',async({page})=>{await page.goto('/100-builds/027/a');await expect(page.getByRole('heading',{name:'CRITICAL DEPENDENCY'})).toBeVisible();await expect(page.getByText(/system the creator does not control/)).toBeVisible()});test('recalculates when control relationships change',async({page})=>{await page.goto('/100-builds/027/a');await page.getByRole('button',{name:'ADD EXPORTS + BACKUPS'}).click();await page.getByRole('listitem').filter({hasText:'DISCOVERY REACH'}).getByRole('button',{name:'OWN'}).click();await expect(page.getByRole('heading',{name:'OWNERSHIP GAPS'})).toBeVisible()});test('exports locally',async({page})=>{await page.goto('/100-builds/027/a');const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT OWNERSHIP MAP'}).click();expect((await dl).suggestedFilename()).toBe('creator-ownership-stack.json')});test('fits 320px',async({page})=>{await page.setViewportSize({width:320,height:800});await page.goto('/100-builds/027/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false)});test('B has keyboard-operable alternatives to drag',async({page})=>{await page.goto('/100-builds/027/b');await page.getByRole('article').filter({hasText:'DISCOVERY REACH'}).getByRole('button',{name:'MOVE TO OWN'}).click();await expect(page.getByRole('status')).toContainText('STACK EXPOSURE 2')})});
