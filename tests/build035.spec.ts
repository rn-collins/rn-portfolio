import {test,expect} from '@playwright/test';test('035-A exposes data loss, repairs and exports',async({page})=>{await page.goto('/100-builds/035/a');await expect(page.getByRole('heading',{name:'Data Exit & Portability Test'})).toBeVisible();await expect(page.getByRole('heading',{name:'DATA LOSS'})).toBeVisible();await page.getByRole('button',{name:'REPAIR EXPORT PACKAGE'}).click();await expect(page.getByRole('heading',{name:'PORTABLE FIXTURE'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT TEST RECORD'}).click();expect((await dl).suggestedFilename()).toBe('data-portability-test-record.json')});test('035-A has no overflow at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/035/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('035-B transfers accessibly',async({page})=>{await page.goto('/100-builds/035/b');const status=page.getByRole('status');await expect(status).toContainText('TRAPPED IN SOURCE');await page.getByRole('button',{name:'TEST NEXT TRANSFER'}).click();await expect(status).toContainText('DATA LEAVES—MEANING BREAKS')});
