import{expect,test}from'@playwright/test';import{BUILD099_CLOCK,DATASETS,exportCommonsResult,rehearseCommons,type CommonsInput}from'../packages/release/src/build099-commons-engine';
const allowed:CommonsInput={datasetId:'water-01',requester:'PUBLIC',purpose:'PLANNING',acceptTerms:true};
test('099 commons engine is deterministic, bounded and fail closed',()=>{expect(DATASETS).toHaveLength(3);const shared=rehearseCommons(allowed);expect(shared.decision).toBe('SHARE');expect(shared.clock).toBe(BUILD099_CLOCK);expect(shared.externalEffect).toBe(false);expect(Object.isFrozen(shared)).toBeTruthy();expect(Object.isFrozen(shared.trace)).toBeTruthy();expect(rehearseCommons(allowed)).toEqual(shared);for(const input of [{...allowed,datasetId:'missing'},{...allowed,acceptTerms:false},{...allowed,purpose:'COMMERCIAL' as const},{...allowed,datasetId:'shelter-03'}]){const result=rehearseCommons(input);expect(result.decision).toBe('BLOCKED');const first=result.trace.findIndex(x=>x.status==='FAIL');expect(first).toBeGreaterThanOrEqual(0);expect(result.trace.slice(first+1).every(x=>x.status==='NOT_RUN')).toBeTruthy()}expect(exportCommonsResult(allowed)).toBe(JSON.stringify(shared,null,2))});
test('099 routes expose bounded native interactions at 320px',async({page})=>{await page.setViewportSize({width:320,height:900});for(const side of ['a','b']){await page.goto('/100-builds/099/'+side);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy();expect(await page.locator('main').getAttribute('data-build')).toBe('099')}});
