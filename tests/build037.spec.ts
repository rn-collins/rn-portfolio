import {test,expect} from '@playwright/test';test('037-A governs every stage and exports',async({page})=>{await page.goto('/100-builds/037/a');await expect(page.getByRole('heading',{name:'Feedback → Change Governance Lab'})).toBeVisible();for(let i=0;i<5;i++)await page.getByRole('button',{name:'COMPLETE NEXT GOVERNED STAGE'}).click();await expect(page.getByRole('heading',{name:'CLOSED LOOP'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT GOVERNANCE RECORD'}).click();expect((await dl).suggestedFilename()).toBe('feedback-change-governance-record.json')});test('037-A has no overflow at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/037/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('037-B moves feedback accessibly',async({page})=>{await page.goto('/100-builds/037/b');const status=page.getByRole('status');await expect(status).toContainText('TRIAGE PENDING');await page.getByRole('button',{name:'MOVE FEEDBACK FORWARD'}).click();await expect(status).toContainText('EVIDENCE PENDING')});
