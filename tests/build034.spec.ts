import {test,expect} from '@playwright/test';test('034-A reconstructs and exports an alternate replay',async({page})=>{await page.goto('/100-builds/034/a');await expect(page.getByRole('heading',{name:'AI Failure & Incident Reconstruction Lab'})).toBeVisible();await expect(page.getByRole('heading',{name:'UNCONTAINED'})).toBeVisible();await page.getByRole('button',{name:'TEST ALL RECOVERY POINTS'}).click();await expect(page.getByRole('heading',{name:'RECONSTRUCTABLE'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT INCIDENT RECORD'}).click();expect((await dl).suggestedFilename()).toBe('ai-incident-reconstruction-record.json')});test('034-A has no overflow at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/034/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('034-B tests intervention accessibly',async({page})=>{await page.goto('/100-builds/034/b');const status=page.getByRole('status');await expect(status).toContainText('INCIDENT REPLAY');await page.getByRole('button',{name:'TEST NEXT INTERVENTION'}).click();await expect(status).toContainText('CONTAIN AT REVIEW')});
