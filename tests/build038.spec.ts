import {test,expect} from '@playwright/test';
test('038-A exposes conservative gates and blocks missing required slices',async({page})=>{await page.goto('/100-builds/038/a');await expect(page.getByRole('heading',{name:'SYNTHETIC COMPARISON COMPLETE'})).toBeVisible();await expect(page.getByText(/worst rare-x-language/).first()).toBeVisible();await page.getByRole('button',{name:'REMOVE RARE SLICES'}).click();await expect(page.getByRole('heading',{name:'EVALUATION BLOCKED'})).toBeVisible();await expect(page.getByRole('alert').first()).toContainText(/slice/);});
test('038-A exports exact replay contract',async({page})=>{await page.goto('/100-builds/038/a');const event=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT REPLAY RECORD'}).click();const dl=await event;expect(dl.suggestedFilename()).toBe('domain-ai-evaluation-record.json');});
test('038-A supports keyboard and 320px without overflow',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/038/a');await page.keyboard.press('Tab');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy();});
test('038-B reranks with accessible live copy',async({page})=>{await page.goto('/100-builds/038/b');const status=page.getByRole('status');await expect(status).toContainText('ONE SCORE HIDES THE TASK');await page.getByRole('button',{name:'CHANGE EVALUATION CONTEXT'}).click();await expect(status).toContainText('CONSEQUENCE CHANGES WEIGHTS');});