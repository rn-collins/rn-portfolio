import {test,expect} from '@playwright/test';test('038-A changes evaluation and exports',async({page})=>{await page.goto('/100-builds/038/a');await expect(page.getByRole('heading',{name:'Domain AI Evaluation Studio'})).toBeVisible();await expect(page.getByRole('heading',{name:'COMPARABLE FIXTURE'})).toBeVisible();await page.getByRole('button',{name:'REMOVE RARE SLICE'}).click();await expect(page.getByRole('heading',{name:'CONTROL GAPS'})).toBeVisible();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'EXPORT EVALUATION RECORD'}).click();expect((await dl).suggestedFilename()).toBe('domain-ai-evaluation-record.json')});test('038-A no overflow at 320px',async({page})=>{await page.setViewportSize({width:320,height:760});await page.goto('/100-builds/038/a');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBeTruthy()});test('038-B reranks accessibly',async({page})=>{await page.goto('/100-builds/038/b');const status=page.getByRole('status');await expect(status).toContainText('ONE SCORE HIDES THE TASK');await page.getByRole('button',{name:'CHANGE EVALUATION CONTEXT'}).click();await expect(status).toContainText('CONSEQUENCE CHANGES WEIGHTS')});
