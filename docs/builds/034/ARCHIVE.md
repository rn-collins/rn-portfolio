# Build 034 archive contract

Replay with engine `034.1.0`, `data/build-034-engine-v1.json`, and the local `ai-incident-reconstruction-record` export. Canonical fields are frozen. Events, timings, controls, alternate outcomes, statuses, and visual copy are derived from a synthetic fixture.
