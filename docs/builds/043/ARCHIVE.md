# Build 043 archive contract

Replay engine `043.2.0` with the exact exported synthetic member graph and role/control bindings. Canon is frozen; fixture roles, capacity, cost, bottlenecks and copy are derived.

Replay is state-only and must not invoke agents, assign work, contact people, change permissions or mutate systems.
