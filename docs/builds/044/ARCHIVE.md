# Build 044 archive contract

Replay engine `044.2.0` with the exact atomic exported synthetic context and component snapshot. Canon is frozen; context, components, versions, dependencies, validity and copy are derived.

Replay is state-only and must not approve work, collect consent, make disclosures, file records or mutate regulated systems.
