# Build 036 archive contract

Replay with engine `036.1.0`, `data/build-036-engine-v1.json`, and a local `offline-survival-record` export. Canonical fields are frozen. Dependencies, capabilities, fallback rules, states, and visual copy are derived from a synthetic fixture.
