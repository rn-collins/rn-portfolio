# Build 035 archive contract

Replay with engine `035.1.0`, `data/build-035-engine-v1.json`, and a local `data-portability-test-record` export. Canonical fields are frozen; schema, mappings, values, scores, statuses, and visual copy are derived from a synthetic fixture.
