# Build 041 archive contract

Replay engine `041.1.0` with the exact exported synthetic nodes and as-of date. Canon is frozen; fixture nodes, roles, assignments, coverage and copy are derived.

Replay is state-only. It must not change directories, permissions, vendor accounts, employment records or real decision authority.
