# Build 041 archive contract

Replay engine `041.2.0` with the exact exported synthetic nodes, evaluation date, and fixture identity/kind/evidence bindings. Canon is frozen; fixture nodes, roles, assignments, coverage and copy are derived.

Replay is state-only. It must not change directories, permissions, vendor accounts, employment records or real decision authority.
