# Build 047 archive

Version 047.2.0 is the hardened deterministic implementation. Its intervention, places, constraints, capacity and costs are invented. The engine exact-binds the fixture, records money as safe-integer cents, rejects malformed or mutated input, and emits deterministic gaps and adaptations. Export atomically preserves place, intervention, result, frozen canon, version and a state-only replay boundary without network activity.
