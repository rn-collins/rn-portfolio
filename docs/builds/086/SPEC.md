# Build 086 — Idea Incubation Through Content System

Canonical public title: **Use the Post as a Prototype**. Build 086 uses 007, 081, and 082 and creates `cap:086`. Its artifacts are `Content Experiment Model` and `Signal-to-Build Rules`. 086-B is **The Post Is a Prototype**: content experiments update hypotheses as signals arrive.

The tool admits five exact fictional ideas and frozen simulated observations. It derives six dimension scores from frozen signals, reads frozen declared synthetic coverage, an ordered decision trace, and a bounded next test. Exact presets only; mutations, malformed input, real audience data, or unrecognized input fail closed.

Possible outcomes are HOLD, REFRAME, TEST DEEPER, NARROW, and READY FOR A BOUNDED REAL-WORLD TEST. The system never calls content attention demand, a waitlist a purchase, stated intent behavior, or simulated behavior real behavior. It does not determine market fit, commercial viability, conversion, revenue, market size, or willingness to pay.

A uses `IDEA PROTOTYPE`, `RUN CONTENT EXPERIMENTS`, `RESTORE QUIET CAREER IDEA`, `EXPORT INCUBATION RECORD`, and `synthetic-idea-incubation-through-content.json`. B begins closed and uses `REVEAL TEST RECORD`. Both preserve negative signals, unknowns, native controls, live status, visible focus, reduced motion, print, and 320px containment.