# Build 040 archive contract

Replay engine `040.2.0` only with the exact exported input, dated `040-policy-2` contract, and current unrevoked Build 019/032/038/039 control records. Canon is frozen; scenarios, thresholds, rules, reasons, and copy are derived.

Replay is state-only and must not invoke tools, contact people, mutate systems, or claim that a modeled action was safe or authorized.
