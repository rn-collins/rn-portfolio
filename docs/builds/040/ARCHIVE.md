# Build 040 archive contract

Replay engine `040.1.0` with the versioned fixture and local `stop-escalate-decision-record`. Canon is frozen; scenarios, thresholds, rules, reasons, and copy are derived.
