# Build 031 archive contract

Replay with `data/build-031-engine-v1.json`, engine `031.1.0`, and a browser-local `privacy-conscious-measurement-record` export. Frozen fields are title, job, B-Web sentence, sequence, and lineage. Derived fields are explicitly labeled. The fixture is synthetic and contains no observed behavior.
