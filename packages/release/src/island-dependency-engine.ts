export type SystemKind='external'|'local'|'essential';export type DependencyNode={id:string;label:string;kind:SystemKind;sector:string;geography:string;dependsOn:string[]};export type ShockResult={status:'OPERATING'|'DEGRADED'|'INVALID MODEL';failed:string[];affectedSectors:string[];affectedGeographies:string[];engineVersion:string;errors:string[]};export const ISLAND_DEPENDENCY_ENGINE_VERSION='046.1.0';
export function simulateIsland(nodes:DependencyNode[],cutId:string|null):ShockResult{const errors:string[]=[];const byId=new Map<string,DependencyNode>();for(const n of nodes){if(!n.id.trim()||byId.has(n.id)||!n.label.trim()||!['external','local','essential'].includes(n.kind)||!n.sector.trim()||!n.geography.trim())errors.push(n.id||'UNNAMED');else byId.set(n.id,n)}for(const n of nodes)for(const d of n.dependsOn)if(!byId.has(d)||d===n.id)errors.push(`${n.id}:DEPENDENCY`);if(cutId!==null&&!byId.has(cutId))errors.push('UNKNOWN CUT');if(errors.length)return{status:'INVALID MODEL',failed:[],affectedSectors:[],affectedGeographies:[],engineVersion:ISLAND_DEPENDENCY_ENGINE_VERSION,errors:[...new Set(errors)]};const failed=new Set<string>();if(cutId)failed.add(cutId);let changed=true;while(changed){changed=false;for(const n of nodes)if(!failed.has(n.id)&&n.dependsOn.some(d=>failed.has(d))){failed.add(n.id);changed=true}}const broken=[...failed];return{status:broken.length?'DEGRADED':'OPERATING',failed:broken,affectedSectors:[...new Set(broken.map(id=>byId.get(id)!.sector))].sort(),affectedGeographies:[...new Set(broken.map(id=>byId.get(id)!.geography))].sort(),engineVersion:ISLAND_DEPENDENCY_ENGINE_VERSION,errors:[]}}
