'use client';
import { useState } from 'react';
export function HumanLoopReveal(){const [step,setStep]=useState(0);return <div className={`visual-frame ${step>0?'step1':''} ${step>1?'step2':''}`} role="button" tabIndex={0} aria-label="Advance Human in the Loop visual reveal" onClick={()=>setStep((step+1)%3)} onKeyDown={e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();setStep((step+1)%3)}}}><div className="visual-stage"><div className="visual-big">HUMAN<br/><span className="visual-accent">IN THE</span><br/>LOOP</div></div><div className="visual-stage"><div className="visual-questions">{['WHO reviews?','WHAT do they inspect?','AGAINST WHICH standard?','WHEN does review happen?','CAN they change the outcome?','WHAT happens when review fails?'].map(q=><div className="visual-q" key={q}>{q}</div>)}</div></div><div className="visual-stage"><article className="visual-post"><div className="eyebrow">RN BUILDS · 001 / 100</div><h2>“Human in the loop” is not a control until the review is designed.</h2><p>I built the Human Review Design Framework to turn a reassuring phrase into explicit review architecture.</p><p><strong>001-A:</strong> the functional tool.<br/><strong>001-B:</strong> this visual build.</p></article></div></div>}
