'use client';
export type ProvenanceRef={id:string;classification:string};
export type ReviewDimension={key:string;label:string;score:0|1|2;note:string;provenance?:ProvenanceRef[]};
export type ReviewAssessment={score:number;maxScore:number;grade:'Strong'|'Partial'|'Weak';gaps:string[];priorities:string[];dimensions:ReviewDimension[];protocol:string;engineVersion?:string};
const sourceLabels:Record<string,string>={n1:'NIST AI RMF 1.0',e1:'EU AI Act Arts. 13–14',i1:'UK ICO Human Review'};
function exportAssessment(assessment:ReviewAssessment){const payload={build:'001-A',createdAt:new Date().toISOString(),engineVersion:assessment.engineVersion??'001.1.0',classification:{dimensions:'direct-source and cross-source synthesis as identified per dimension',compositeScore:'product-heuristic'},assessment,evidenceManifest:'data/build-001-evidence-v1.json',limitations:['not a compliance determination','not proof the control works in practice','validate requirements for jurisdiction, sector and use case']};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='build-001-human-review-record.json';a.click();URL.revokeObjectURL(url)}
export function ProtocolResult({assessment}:{assessment:ReviewAssessment}){return <section className="result result-rich" aria-live="polite" id="generated-protocol"><div className="result-head"><div><div className="eyebrow">60-second diagnosis</div><h2>Your oversight architecture</h2>{assessment.engineVersion&&<small>engine / {assessment.engineVersion}</small>}</div><div className={`score score-${assessment.grade.toLowerCase()}`}><strong>{assessment.score}/{assessment.maxScore}</strong><span>{assessment.grade}</span></div></div><div className="dimension-grid">{assessment.dimensions.map(d=><div className="dimension" key={d.key}><div><strong>{d.label}</strong><span>{d.note}</span>{d.provenance?.length?<small>basis / {d.provenance.map(p=>sourceLabels[p.id]??p.id).join(' · ')}</small>:null}</div><div className={`dimension-meter meter-${d.score}`} aria-label={`${d.label}: ${d.score} of 2`}>{[0,1].map(i=><i key={i} className={i<d.score?'on':''}/>)}</div></div>)}</div>{assessment.priorities.length>0&&<div className="priority-panel"><div className="eyebrow">Fix these first</div><ol>{assessment.priorities.slice(0,3).map(p=><li key={p}>{p}</li>)}</ol></div>}{assessment.gaps.length>3&&<details className="gap-panel"><summary>See all {assessment.gaps.length} control gaps</summary><ul>{assessment.gaps.map(g=><li key={g}>{g}</li>)}</ul></details>}<div className="protocol-head"><div><div className="eyebrow">Design the control</div><h3>Review protocol</h3></div><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><button className="copy-btn" type="button" onClick={()=>navigator.clipboard?.writeText(assessment.protocol)}>Copy protocol</button><button className="copy-btn" type="button" onClick={()=>exportAssessment(assessment)}>Export JSON record</button></div></div><pre className="protocol-output">{assessment.protocol}</pre><p className="result-note">Design aid only. The composite score and thresholds are product heuristics, not requirements from the cited authorities. A strong architecture score means the control is more explicit and challengeable; it does not certify legal compliance or prove the control works in practice. Validate requirements for your jurisdiction, sector, and use case.</p></section>}
