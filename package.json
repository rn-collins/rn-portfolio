{
  "name": "rn-builds-platform",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "turbo dev --filter=@rn/web",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test:e2e": "playwright test",
    "ci": "pnpm typecheck && pnpm lint && pnpm build && pnpm test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "turbo": "latest",
    "typescript": "latest"
  }
}
