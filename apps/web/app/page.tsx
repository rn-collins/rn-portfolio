import type {Metadata} from 'next';import Link from 'next/link';import {builds} from '../../../packages/registry/src/index';import s from './exhibit.module.css';
export const metadata:Metadata={title:'RN Collins — Public Build Exhibition',description:'A public exhibition of useful systems, experiments, and visual builds by RN Collins.',alternates:{canonical:'/'},openGraph:{title:'RN Collins — Public Build Exhibition',description:'100 useful things, built in public.',type:'website',images:[{url:'/og-image.png',width:1200,height:630,alt:'RN Collins Public Build Exhibition'}]},twitter:{card:'summary_large_image',title:'RN Collins — Public Build Exhibition',description:'100 useful things, built in public.',images:['/og-image.png']}};
export default function Home(){const released=builds.filter(b=>b.publicState==='OnView');const lab=builds.find(b=>b.publicState==='InLab');const next=builds.filter(b=>b.publicState==='ComingNext');const releasedRange=released.length?`${released[0].id}${released.length>1?`–${released.at(-1)?.id}`:''}`:'NONE';const nextRange=next.length?`${next[0].id}${next.length>1?`–${next.at(-1)?.id}`:''}`:'NONE';return <main className={s.space}><section className={s.room}><div className={s.label}>RN COLLINS<br/>PUBLIC BUILD EXHIBITION<br/>2026—</div><div className={s.wall}><div><h1>I NOTICE<br/>WHAT&apos;S<span>missing.</span></h1></div><div className={s.copy}><p>Then I build it.</p><small>I’m building 100 useful things from questions I keep running into across technology, law, neuroscience, place, research, work, and everyday life.<br/><br/><strong>You do not need to be a technologist, lawyer, scientist, or expert to understand or use what is here.</strong><br/><br/>Released builds stay on view. The current build stays visible while it is tested. The frozen sequence ahead remains open to inspect.</small></div></div><div className={s.plinth}>EXHIBITION STATUS<br/><br/>{releasedRange} / ON VIEW<br/>{lab?`${lab.id} / IN THE LAB`:null}<br/>{nextRange} / COMING NEXT</div><div className={s.doors}><Link className={s.door} href="/100-builds">ENTER THE 100 →</Link><Link className={s.doorAlt} href="/practice">WORK WITH RN →</Link></div></section><section className={s.guide}><h2>Come for whatever is useful.</h2><div className={s.guideGrid}><div><strong>USE THE BUILDS</strong><p>Try released tools and experiments. The first path is written for people who do not already know the field.</p></div><div><strong>LOOK INSIDE THE LAB</strong><p>The current build remains visible while it is tested, so the practice is public without pretending unfinished work is released.</p></div><div><strong>LEARN FROM THE PROCESS</strong><p>See what changed, what failed, the evidence behind decisions, reusable methods, and how later projects inherit earlier work.</p></div></div></section></main>}