'use client';import {useState} from 'react';import s from '../feedback.module.css';const stages=['TRIAGE ASSERTION','SCOPED EVIDENCE','AUTHORIZED DECISION','LINKED CHANGE','MEASURED OUTCOME RECORD'];export default function FeedbackJourney(){const [step,setStep]=useState(0),complete=step===stages.length;return <section className={s.story}><header><span>BUILD 037-B</span><p>A REPORT IS THE START</p><h1>Feedback travels through triage, evidence, decision, change and measured outcome.</h1></header><div className={s.journey} data-step={step} aria-label={`Synthetic feedback journey: ${step} of ${stages.length} records present`}>{stages.map((x,i)=><i key={x} className={i<step?s.done:''}>{x}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>{step} / {stages.length} RECORDS PRESENT</small><h2>{complete?'GOVERNANCE RECORD COMPLETE':`${stages[step]} PENDING`}</h2><p>{complete?'Structural records return to the report; remedy, fairness, effectiveness, and affected-person outcome remain unproven.':`The synthetic record cannot skip ${stages[step].toLowerCase()}.`}</p></article><div className={s.actions}><button disabled={step===0} onClick={()=>setStep(v=>Math.max(0,v-1))}>PREVIOUS STAGE</button><button onClick={()=>setStep(v=>complete?0:v+1)}>{complete?'RESET JOURNEY':'NEXT STAGE'}</button></div><p className={s.note}>Synthetic governance story—not report truth, SLA performance, remedy, retaliation safety, fairness, compliance, or real measured outcome.</p></section>}
