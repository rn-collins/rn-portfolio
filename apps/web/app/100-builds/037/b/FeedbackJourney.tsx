'use client';import {useState} from 'react';import s from '../feedback.module.css';const stages=['TRIAGE','EVIDENCE','DECISION','CHANGE','OUTCOME'];export default function FeedbackJourney(){const [step,setStep]=useState(0);const closed=step===stages.length;return <section className={s.story}><header><span>BUILD 037-B</span><p>A REPORT IS THE START</p><h1>Feedback travels through triage, evidence, decision, change and measured outcome.</h1></header><div className={s.journey} data-step={step} aria-hidden="true">{stages.map((x,i)=><i key={x} className={i<step?s.done:''}>{x}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>{step} / 5 GOVERNED STAGES</small><h2>{closed?'LOOP CLOSED':`${stages[step]} PENDING`}</h2><p>{closed?'The outcome returns to the original report with a record.':`Feedback cannot skip ${stages[step].toLowerCase()} and still claim a governed change.`}</p></article><button onClick={()=>setStep(v=>closed?0:v+1)}>{closed?'RESET FEEDBACK':'MOVE FEEDBACK FORWARD'}</button><p className={s.note}>Derived synthetic governance story; closure does not prove remedy, effectiveness, fairness, or compliance.</p></section>}
