import type {Metadata} from 'next';
import Link from 'next/link';
import {notFound} from 'next/navigation';
import {buildArchives} from '../../_archive/archive-data';
import {archiveSourceHref} from '../../_archive/public-documents';
import {getLinkedInMedia} from '../../_archive/media';
import s from './archive.module.css';
export function generateStaticParams(){return Object.keys(buildArchives).map(id=>({id}));}
export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const {id}=await params;const a=buildArchives[id];if(!a)return {title:'Archive not found'};const title=`Build ${id} Archive — ${a.title}`;const description=`Durable research, decisions, artifacts, audits, source files, plans and media for Build ${id}.`;return {title,description,alternates:{canonical:`/100-builds/${id}/archive`},openGraph:{title,description,type:'website'},twitter:{card:'summary',title,description}}}
function Items({items}:{items:typeof buildArchives[string]['research']}){return <div className={s.items}>{items.map((item,i)=>{const source=item.path?archiveSourceHref(item.path):null;return <article key={`${item.label}-${i}`}><div className={s.itemTop}><b>{item.label}</b>{item.path&&<code>{item.path}</code>}</div><p>{item.detail}</p><div>{item.href&&<Link href={item.href}>OPEN RELATED WEBSITE SURFACE →</Link>}{source&&<Link href={source}>OPEN FULL RETAINED RECORD →</Link>}</div></article>})}</div>}
export default async function BuildArchivePage({params}:{params:Promise<{id:string}>}){const {id}=await params;const a=buildArchives[id];if(!a)notFound();const media=getLinkedInMedia(id);return <main className={s.page}><header className={s.mast}><Link href={`/100-builds/${id}`}>← BUILD {id}</Link><Link href="/100-builds/archive">PROGRAM ARCHIVE</Link></header><section className={s.hero}><span>DURABLE BUILD ARCHIVE / 001–006 BACKFILL</span><h1>BUILD {id}<br/>{a.title}</h1><p>{a.thesis}</p><div className={s.question}><b>QUESTION</b><span>{a.question}</span></div></section><section className={s.rule}><b>CHAT-INDEPENDENCE RULE</b><p>This page exists so the durable record does not depend on conversation history. Research, decisions, generated artifacts, audits, limitations, media metadata, plans and canonical source paths are retained here as part of the build itself.</p></section><section className={s.section}><span>01 / RESEARCH + EVIDENCE</span><h2>What informed the build</h2><Items items={a.research}/></section><section className={s.section}><span>02 / CREATED</span><h2>What exists because we built it</h2><Items items={a.created}/></section><section className={s.section}><span>03 / ARCHITECTURE + DECISIONS</span><h2>Why it works this way</h2><Items items={a.decisions}/></section><section className={s.section}><span>04 / AUDIT + RELEASE</span><h2>How the work was checked</h2><Items items={a.audits}/></section><section className={s.section}><span>05 / PLANS + OPEN THREADS</span><h2>What this build intentionally leaves forward</h2><Items items={a.plans}/></section><section className={s.section}><span>06 / MEDIA</span><h2>Take the social artifact with you</h2>{media?<article className={s.media}><div className={s.preview}><video controls muted playsInline preload="metadata" poster={media.poster} aria-label={`Build ${id} LinkedIn film preview`}><source src={media.path} type="video/mp4"/></video></div><div className={s.mediaInfo}><b>B-LINKEDIN / CANONICAL FILM</b><h3>{media.width}×{media.height} · 4:5 MP4 · {media.fps} fps · silent-first</h3><p>{media.durationSeconds} seconds · {(media.bytes/1024).toFixed(1)} KB</p><p><strong>FINAL PROPOSITION /</strong> {media.finalProposition}</p><code>SHA-256 {media.sha256}</code><small>Reproducible from {media.spec} with {media.generator}. The website—not a chat attachment—is the canonical media source.</small></div><div className={s.mediaActions}><a href={media.path} download={`build-${id}-linkedin.mp4`}>DOWNLOAD MP4 ↓</a><a href={media.poster} download={`build-${id}-linkedin-poster.png`}>DOWNLOAD POSTER ↓</a></div></article>:<p>No canonical film is registered for this build.</p>}</section><section className={s.section}><span>07 / CANONICAL SOURCE INVENTORY</span><h2>The repository files that preserve the implementation record</h2><div className={s.files}>{a.sourceFiles.map(path=><code key={path}>{path}</code>)}</div><p className={s.note}>These paths are retained as canonical implementation evidence. Publicly approved research, evidence, method, audit and production records are readable in full through the archive reader; implementation paths remain listed here so the live artifact and its source record stay traceable.</p></section><footer className={s.footer}><Link href={`/100-builds/${id}/record`}>PUBLIC BUILD RECORD →</Link><Link href={`/100-builds/${id}`}>RETURN TO BUILD {id} →</Link></footer></main>}
