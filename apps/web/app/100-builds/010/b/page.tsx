import {buildVariantMetadata} from '../../_metadata';
export const metadata=buildVariantMetadata('010','b');
import type {Metadata} from 'next';import Link from 'next/link';import SkeletonGraph from './SkeletonGraph';import shell from '../../001/room.module.css';export default function Build010B(){return <main className={shell.toolRoom}><nav className={shell.crumb}><Link href="/100-builds/010">← Build 010</Link><span>010-B / Web expression</span></nav><header className={shell.toolLabel}><div><span>BUILD 010-B</span><h2>From Words to Structure</h2></div><p>Move the same idea from a paragraph to named entities, fields, relationships, constraints, and source rules.</p></header><SkeletonGraph/></main>}
