import {buildVariantMetadata} from '../../_metadata';\nexport const metadata=buildVariantMetadata('010','a');\nimport type {Metadata} from 'next';import Link from 'next/link';import IdeaSkeletonLab from './IdeaSkeletonLab';import shell from '../../001/room.module.css';

export default function Build010A(){return <main className={shell.toolRoom}><nav className={shell.crumb}><Link href="/100-builds/010">← Build 010</Link><span>010-A / Functional build</span></nav><header className={shell.toolLabel}><div><span>BUILD 010-A</span><h1>Give the Idea a Skeleton</h1></div><p>Define the things, their fields, how they relate, and the rules that make every fact traceable and testable.</p></header><IdeaSkeletonLab/></main>}
