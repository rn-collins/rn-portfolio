import type {ReactNode} from 'react';
import BExperience from '../../_components/BExperience';
export default function Layout({children}:{children:ReactNode}){return <BExperience id="034">{children}</BExperience>}
