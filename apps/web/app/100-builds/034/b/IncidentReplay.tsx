'use client';import {useState} from 'react';import s from '../incident.module.css';const scenes=[{title:'INCIDENT REPLAY',body:'A wrong classification crosses routing and decision dependencies.',cut:0},{title:'CONTAIN AT REVIEW',body:'A required review queue interrupts propagation before the outcome.',cut:2},{title:'ROLL BACK THE OUTCOME',body:'A tested rollback restores the prior state while investigation continues.',cut:3}];export default function IncidentReplay(){const [step,setStep]=useState(0);const scene=scenes[step];return <section className={s.story}><header><span>BUILD 034-B</span><p>FAILURE LEAVES A SHAPE</p><h1>A timeline and dependency graph replay the incident and alternate rollback points.</h1></header><div className={s.graph} data-cut={scene.cut} aria-hidden="true">{['INTAKE','CLASSIFY','ROUTE','DECIDE','INCIDENT'].map((n,i)=><i key={n} data-i={i}>{n}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>REPLAY {step+1} / 3</small><h2>{scene.title}</h2><p>{scene.body}</p></article><button onClick={()=>setStep(v=>(v+1)%scenes.length)}>{step===2?'RESET INCIDENT':'TEST NEXT INTERVENTION'}</button><p className={s.note}>Derived synthetic replay; it does not prove root cause, fault, prevention efficacy, or a forensic conclusion.</p></section>}
