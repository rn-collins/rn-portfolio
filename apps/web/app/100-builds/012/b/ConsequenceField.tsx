'use client';import {useState} from 'react';import s from './consequence-field.module.css';
const states={LOW:{score:24,title:'DOCUMENT THE USE.',body:'Name the owner, purpose, test evidence, monitoring, and record.'},MATERIAL:{score:58,title:'ADD HUMAN CONTROL.',body:'Require review triggers, change authority, stop rules, and outcome monitoring.'},SEVERE:{score:88,title:'ESCALATE / DO NOT DEPLOY.',body:'Rights or essential-service consequences require appeal, incident response, notice, rollback, and independent review.'}} as const;
export default function ConsequenceField(){const [key,setKey]=useState<keyof typeof states>('MATERIAL');const x=states[key];return <section className={s.field}><div className={s.controls}>{(Object.keys(states) as Array<keyof typeof states>).map(k=><button key={k} aria-pressed={k===key} onClick={()=>setKey(k)}>{k} CONSEQUENCE</button>)}</div><div className={s.ring} style={{'--score':x.score} as React.CSSProperties}><span>{x.score}</span><small>/ 100</small></div><div className={s.copy}><p>CONTROL LOAD RECONFIGURED</p><h2>{x.title}</h2><p>{x.body}</p><p className={s.boundary}>A heuristic can expose missing controls. It cannot approve the use.</p></div></section>}