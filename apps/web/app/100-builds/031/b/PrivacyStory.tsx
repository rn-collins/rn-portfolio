'use client';import {useState,type CSSProperties} from 'react';import s from '../privacy.module.css';
const states=[{title:'HIGH-EXPOSURE PLAN',body:'Synthetic replay, location, search, and identifier fields create linkability and sensitive-inference risks. Scores are planning ordinals, not measurements.',coverage:100,privacy:96,identify:100},{title:'PURPOSE REVIEW',body:'Remove fields that do not support one of the four named product decisions. Purpose is distinct from consent and legal basis.',coverage:100,privacy:74,identify:64},{title:'DECLARED MINIMUM SET',body:'Four derived count events cover the declared decisions with less modeled exposure. Necessity, anonymity, deletion, and lawfulness remain unproven.',coverage:100,privacy:22,identify:24}];
export default function PrivacyStory(){const [step,setStep]=useState(0),current=states[step];return <section className={s.story}><header><span>BUILD 031-B</span><p>THE PRODUCT IS WATCHING</p><h2>Toggle events and watch insight versus privacy and identifiability change.</h2></header><div className={s.field} aria-label={`Synthetic telemetry plan: ${current.title}`} data-step={step}><div className={s.person}>SYNTHETIC PLAN</div>{['REPLAY','LOCATION','SEARCH','FEATURE','COMPLETE','ERROR'].map((event,index)=><i key={event} style={{'--i':index} as CSSProperties}>{event}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>VIEW {step+1} / 3</small><h2>{current.title}</h2><p>{current.body}</p><div className={s.storyMeters}><span>DECISION COVERAGE <b>{current.coverage}</b></span><span>PRIVACY LOAD <b>{current.privacy}</b></span><span>IDENTIFIABILITY <b>{current.identify}</b></span></div></article><div className={s.actions}><button disabled={step===0} onClick={()=>setStep(v=>Math.max(0,v-1))}>PREVIOUS VIEW</button><button onClick={()=>setStep(v=>(v+1)%states.length)}>NEXT VIEW</button></div><p className={s.note}>Derived synthetic planning demonstration. It collects no behavior and does not prove insight, necessity, anonymity, deletion, valid consent, legal basis, or privacy compliance. Contextual legal, security, data-governance, and production-system verification remain required.</p></section>}
