'use client';import {useState} from 'react';import s from './process-drag.module.css';const states=[{label:'SUBMIT',cost:'ONE REQUEST',line:'The institution receives what it actually needs.'},{label:'WAIT',cost:'96 HOURS',line:'A queue without a visible owner turns time into uncertainty.'},{label:'REPEAT',cost:'3 TIMES',line:'The same evidence is requested after it was already accepted.'},{label:'DECIDE',cost:'ONE NOTICE',line:'The outcome arrives with reasons, next steps, and a real response path.'}];export default function ProcessDrag(){const [active,setActive]=useState(1);return <section className={s.wrap}><header><p>INSTITUTIONAL PROCESS / EXPERIENCED PATH</p><h2>THE PROCESS IS PART OF THE OUTCOME.</h2></header><div className={s.rail}>{states.map((state,index)=><button key={state.label} aria-pressed={index===active} onClick={()=>setActive(index)}><b>0{index+1}</b><span>{state.label}</span><i>{state.cost}</i></button>)}</div><article><p>SELECTED STATE / {states[active].label}</p><h2>{states[active].cost}</h2><strong>{states[active].line}</strong><button onClick={()=>setActive(active===1||active===2?3:1)}>{active===1||active===2?'SIMULATE REPAIR':'REVEAL FRICTION'}</button></article></section>}
