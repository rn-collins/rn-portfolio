'use client';import{useState}from'react';import{inspectSpace,type SpaceScene}from'../../../../../../packages/release/src/space-effect-engine';import s from'../space.module.css';const states:SpaceScene[]=[{fixtureId:'SYNTHETIC-SPACE-01',sceneId:'OPEN_OFFICE',goal:'FOCUS',light:3,sound:3,enclosure:0,nature:0},{fixtureId:'SYNTHETIC-SPACE-01',sceneId:'QUIET_ROOM',goal:'FOCUS',light:1,sound:1,enclosure:2,nature:2},{fixtureId:'SYNTHETIC-SPACE-01',sceneId:'QUIET_ROOM',goal:'CONNECT',light:1,sound:1,enclosure:2,nature:2}];export default function SpaceStory(){const[i,setI]=useState(0);const r=inspectSpace(states[i]);return <section className={s.story}><div role="status" aria-live="polite" aria-atomic="true"><small>STATE {i+1}/3 · {states[i].sceneId.replace('_',' ')} · {states[i].goal}</small><h2>{r.status}</h2><p>{r.tradeoffs.join(' · ')}</p></div><div className={s.room} aria-label={`Synthetic modeled load ${r.load} of 100`}><span>LIGHT</span><span>SOUND</span><b>{r.load}</b><span>LAYOUT</span><span>NATURE</span></div><button onClick={()=>setI(v=>(v+1)%states.length)}>CHANGE ROOM OR GOAL</button><p className={s.note}>Synthetic demonstration only. The displayed load and tradeoffs belong to this fictional model; light, sound, layout and nature remain separate fictional inputs. No exposure, outcome, population, setting, evidence-strength, causal, universal, accessibility, health, or design-prescription claim is produced; null and contradictory evidence are not modeled.</p></section>}
