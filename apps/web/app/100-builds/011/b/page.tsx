import {buildVariantMetadata} from '../../_metadata';\nexport const metadata=buildVariantMetadata('011','b');\nimport type {Metadata} from 'next';import Link from 'next/link';import IdentityField from './IdentityField';import shell from '../../001/room.module.css';export default function Build011B(){return <main className={shell.toolRoom}><nav className={shell.crumb}><Link href="/100-builds/011">← Build 011</Link><span>011-B / Web expression</span></nav><header className={shell.toolLabel}><div><span>BUILD 011-B</span><h1>Same Name Is Not Same Entity</h1></div><p>Add and remove evidence. Watch two records remain separate, enter review, or become a merge candidate—without hiding the accountable human decision.</p></header><IdentityField/></main>}
