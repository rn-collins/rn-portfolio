'use client';import{useState}from'react';import{buildInterviewNetwork,INTERVIEW_SCENARIOS,type ScenarioId,INTERVIEW_NETWORK_ENGINE_VERSION}from'../../../../../../packages/release/src/interview-network-engine';import s from'../interview.module.css';const ids:ScenarioId[]=['COMPLETE-RECORDED','TIMING-CONFLICT','CONSENT-UNKNOWN','EVIDENCE-MISSING','EDITED-TEXT'];export default function InterviewLab(){const[id,setId]=useState<ScenarioId>('COMPLETE-RECORDED');const input=INTERVIEW_SCENARIOS[id],r=buildInterviewNetwork(input);function download(){const snap={build:'062',version:INTERVIEW_NETWORK_ENGINE_VERSION,fixture:'synthetic',canonical:{title:'The Interview Is More Than a Transcript',job:'Turn interviews into people, claims, themes, evidence, contradictions and follow-up questions.',bWeb:'A transcript becomes a source-linked knowledge network with audio and text provenance.',uses:['011','028','058'],creates:['cap:062']},input:structuredClone(input),result:structuredClone(r),replay:{instruction:'Run buildInterviewNetwork(input) with engine 062.1.0; fixed synthetic audio-text records only; no biometric, identity, intent, truth, consent, rights, credibility, representativeness, authenticity, or admissibility inference.',sideEffects:false,mode:'state-only'},boundary:'Synthetic interview provenance only; no real audio, transcript, speaker, consent, rights or evidence.'};const u=URL.createObjectURL(new Blob([JSON.stringify(snap,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=u;a.download='synthetic-interview-network.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),0)}return <section className={s.lab}><header role="status" aria-live="polite" aria-atomic="true"><small>{id}</small><h2>{r.status}</h2><p>{r.people.join(' · ')}</p></header><div className={s.controls}><label>INTERVIEW STATE<select value={id} onChange={e=>setId(e.target.value as ScenarioId)}>{ids.map(x=><option key={x}>{x.replaceAll('-',' ')}</option>)}</select></label><button onClick={()=>setId('COMPLETE-RECORDED')}>RESTORE RECORDED NETWORK</button><button onClick={download}>EXPORT INTERVIEW NETWORK</button></div><div className={s.columns}><article><small>CLAIMS + THEMES</small>{r.claims.map(x=><p key={x}>{x}</p>)}</article><article><small>AUDIO + TEXT PROVENANCE</small>{r.provenance.map(x=><p key={x}>{x}</p>)}</article></div><aside><b>Evidence:</b> {r.evidence.join(' · ')}<br/><b>Tensions:</b> {r.contradictions.join(' · ')||'None recorded.'}<br/><b>Follow-ups:</b> {r.followUps.join(' · ')}</aside><p className={s.note}><b>Boundary:</b> Speaker labels are not identity or biometric findings. Transcript text is not verified verbatim audio. Themes, claims, tensions, consent/rights fields and evidence links are synthetic assertions—not truth, intent, credibility, permission or completeness.</p></section>}