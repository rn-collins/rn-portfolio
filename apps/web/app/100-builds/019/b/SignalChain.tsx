'use client';import {useState} from 'react';import s from './signal-chain.module.css';const stages=[{label:'100 SIGNALS',sub:'NOISE',detail:'Repeated events compete for attention before anyone knows whether they describe the same condition.'},{label:'1 INCIDENT',sub:'CORRELATE',detail:'Shared evidence, time, affected process, and correlation key collapse duplicates without deleting their record.'},{label:'1 OWNER',sub:'AUTHORITY',detail:'The route chooses a person who can perform the required action—not merely someone who is online.'},{label:'10 MINUTES',sub:'ACKNOWLEDGE',detail:'A visible timer either records acknowledgement or moves the event to a named backup.'}];export default function SignalChain(){const [active,setActive]=useState(0);return <section className={s.wrap}><header><p>EVENT → CONSEQUENCE → AUTHORITY → ACTION</p><h1>THE ALERT THAT KNOWS WHO CARES.</h1></header><div className={s.signals} aria-label="Escalation stages">{stages.map((stage,index)=><button key={stage.label} aria-pressed={index===active} onClick={()=>setActive(index)}><b>0{index+1}</b><span>{stage.label}</span><i>{stage.sub}</i></button>)}</div><article><p>SELECTED STAGE / {stages[active].sub}</p><h2>{stages[active].label}</h2><strong>{stages[active].detail}</strong><button onClick={()=>setActive((active+1)%stages.length)}>ADVANCE THE SIGNAL</button></article></section>