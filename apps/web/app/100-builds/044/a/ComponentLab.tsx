'use client';import{useMemo,useState}from'react';import{contextKey,inspectAssembly,type RegulatedComponent,type RegulatedContext}from'../../../../../../packages/release/src/regulated-component-engine';import s from'../components.module.css';
const base:RegulatedContext={jurisdiction:'HYPOTHETICAL-A',useCase:'synthetic intake',consequence:'high',dataClass:'synthetic-sensitive',asOf:'2026-08-20'};function parts(context:RegulatedContext):RegulatedComponent[]{const key=contextKey(context);return[{id:'APPROVAL-1',kind:'approval',version:'1.0',contextKey:key,evidenceId:'E-APPROVAL',reviewedAt:'2026-08-20',expiresAt:'2027-08-20',dependencies:['EVIDENCE-1']},{id:'CONSENT-1',kind:'consent',version:'1.0',contextKey:key,evidenceId:'E-CONSENT',reviewedAt:'2026-08-20',expiresAt:'2027-08-20',dependencies:[]},{id:'EVIDENCE-1',kind:'evidence',version:'1.0',contextKey:key,evidenceId:'E-EVIDENCE',reviewedAt:'2026-08-20',expiresAt:'2027-08-20',dependencies:[]},{id:'DISCLOSURE-1',kind:'disclosure',version:'1.0',contextKey:key,evidenceId:'E-DISCLOSURE',reviewedAt:'2026-08-20',expiresAt:'2027-08-20',dependencies:['CONSENT-1']},{id:'RECORD-1',kind:'record',version:'1.0',contextKey:key,evidenceId:'E-RECORD',reviewedAt:'2026-08-20',expiresAt:'2027-08-20',dependencies:['APPROVAL-1','DISCLOSURE-1']}]}
export default function ComponentLab(){const[changed,setChanged]=useState(false);const[rebuilt,setRebuilt]=useState(false);const context:RegulatedContext=changed?{...base,jurisdiction:'HYPOTHETICAL-B'}:base;const components=useMemo(()=>parts(rebuilt?context:base),[context,rebuilt]);const result=useMemo(()=>inspectAssembly(context,components),[context,components]);function download(){const data={build:'044',version:'044.1.0',fixture:'synthetic',canonical:{title:'Reuse the Parts That Regulation Keeps Requiring',job:'Turn recurring approvals, consent, evidence, disclosures and records into reusable regulated components.',bWeb:'Assemble a regulated workflow from components; change context and see invalidation.',uses:['010','014','017','020','023','029'],creates:['cap:044']},context,components,result,replay:{sideEffects:false},boundary:'Schema-valid synthetic assembly; not legal compliance, approval, valid consent or evidence sufficiency.'};const url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='regulated-component-assembly.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),0)}return <section className={s.lab}><header role="status" aria-live="polite"><small>{context.jurisdiction} · {result.coverage}%</small><h2>{result.status}</h2><p>{result.invalid.join(', ')||'Every declared component is bound to this exact synthetic context.'}</p></header><div className={s.actions}><button aria-pressed={changed} onClick={()=>{setChanged(v=>!v);setRebuilt(false)}}>{changed?'RESTORE CONTEXT':'CHANGE JURISDICTION'}</button><button disabled={!changed} onClick={()=>setRebuilt(true)}>REVALIDATE COMPONENTS</button><button onClick={download}>EXPORT ASSEMBLY</button></div><div className={s.grid}>{components.map(x=><article key={x.id}><small>{x.kind.toUpperCase()}</small><h3>{x.id}</h3><p>Version {x.version}</p><p>{x.evidenceId}</p></article>)}</div><p className={s.note}><b>Boundary:</b> Context binding makes invalidation visible. It does not determine the law, approve reuse, validate consent, or establish that evidence and disclosures satisfy real duties.</p></section>}
