'use client';import{useState}from'react';import s from'../components.module.css';const states=[['ASSEMBLE','Approval · consent · evidence · disclosure · record'],['CONTEXT CHANGE','Jurisdiction changes while components stay fixed'],['INVALIDATED','Context-bound components refuse silent reuse'],['REVALIDATED','Reviewed versions bind to the new declared context']];export default function AssemblyStory(){const[step,setStep]=useState(0);return <section className={s.story}><header><span>BUILD 044-B</span><p>REUSABLE DOES NOT MEAN UNIVERSAL</p><h2>Assemble a regulated workflow from components; change context and see invalidation.</h2></header><div className={s.assembly} data-step={step} aria-hidden="true"><i>APPROVAL</i><i>CONSENT</i><i>EVIDENCE</i><i>DISCLOSURE</i><i>RECORD</i><b/></div><article role="status" aria-live="polite"><small>ASSEMBLY STATE {step+1}/4</small><h2>{states[step][0]}</h2><p>{states[step][1]}</p></article><button onClick={()=>setStep(v=>(v+1)%4)}>{step===3?'RESET ASSEMBLY':'CHANGE CONTEXT'}</button><p className={s.note}>Synthetic component model; not legal advice, approval or proof of compliance.</p></section>}
