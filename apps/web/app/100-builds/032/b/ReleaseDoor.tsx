'use client';import {useState} from 'react';import s from '../release.module.css';const gates=['CONSEQUENCE','WORKFLOW','EVIDENCE','INCLUSION','PRIVACY'];export default function ReleaseDoor(){const [passed,setPassed]=useState(0);const open=passed===gates.length;return <section className={s.story}><header><span>BUILD 032-B</span><p>RELEASE IS A PHYSICAL BOUNDARY</p><h1>Missing tests and evidence physically block release.</h1></header><div className={`${s.door} ${open?s.open:''}`} aria-hidden="true"><div>PRODUCTION</div>{gates.map((g,i)=><i key={g} className={i<passed?s.gone:''}>{g}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>{passed} / {gates.length} GATES CLEARED</small><h2>{open?'EVIDENCE PATH OPEN':'RELEASE BLOCKED'}</h2><p>{open?'All derived fixture gates are present; accountable authorization is still separate.':`${gates[passed]} evidence prevents the system from crossing into consequential use.`}</p></article><button onClick={()=>setPassed(value=>open?0:value+1)}>{open?'RESET THE HARNESS':`CLEAR ${gates[passed]} GATE`}</button><p className={s.note}>Derived synthetic demonstration. An open door does not authorize deployment, certify safety, or prove legal compliance.</p></section>}
