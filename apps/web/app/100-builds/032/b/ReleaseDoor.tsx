'use client';import {useState} from 'react';import s from '../release.module.css';const gates=['SCOPE + 012','WORKFLOW + 013','EVIDENCE + 020','INCLUSION + 030','PRIVACY + 031','ROLLBACK','HUMAN REVIEW'];export default function ReleaseDoor(){const [passed,setPassed]=useState(0),open=passed===gates.length;return <section className={s.story}><header><span>BUILD 032-B</span><p>READINESS IS A BOUNDARY</p><h2>Missing tests and evidence physically block release.</h2></header><div className={`${s.door} ${open?s.open:''}`} aria-label={`Synthetic readiness door: ${passed} of ${gates.length} records present` }><div>AUTHORIZED GOVERNANCE REMAINS</div>{gates.map((g,i)=><i aria-hidden="true" key={g} className={i<passed?s.gone:''}>{g}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>{passed} / {gates.length} RECORDS PRESENT</small><h2>{open?'READINESS RECORD COMPLETE':'READINESS BLOCKED'}</h2><p>{open?'The derived record is complete; approval, deployment authorization, evidence validity, and real-world safety remain separate.':`${gates[passed]} is missing from the synthetic readiness record.`}</p></article><div className={s.actions}><button disabled={passed===0} onClick={()=>setPassed(v=>Math.max(0,v-1))}>RESTORE BLOCKER</button><button onClick={()=>setPassed(v=>open?0:v+1)}>{open?'RESET RECORD':`ADD ${gates[passed]} RECORD`}</button></div><p className={s.note}>Synthetic deterministic demonstration. A complete record is not approval, authorization, certification, legal compliance, or proof that evidence remains valid, applicable, unrevoked, or reproducible.</p></section>}
