'use client';import {useState} from 'react';import s from './risk-circuit.module.css';const nodes=['INTAKE','DATA','MODEL','DECISION','ACTION','OUTCOME'];export default function RiskCircuit(){const [broken,setBroken]=useState<number|null>(null);return <section className={s.circuit}><div className={s.actions}><button onClick={()=>setBroken(null)} aria-pressed={broken===null}>ALL CONTROLS PRESENT</button><button onClick={()=>setBroken(2)} aria-pressed={broken===2}>BREAK MODEL CONTROL</button><button onClick={()=>setBroken(4)} aria-pressed={broken===4}>BREAK ACTION CONTROL</button></div><div className={s.nodes}>{nodes.map((n,i)=><div key={n} className={broken!==null&&i>=broken?s.hot:''}><span>{String(i+1).padStart(2,'0')}</span><h2>{n}</h2><p>{broken===i?'CONTROL GAP':broken!==null&&i>broken?'PROPAGATED':'CONTROLLED'}</p>{i<nodes.length-1&&<b>→</b>}</div>)}</div><div className={s.message}><p>{broken===null?'RISK CONTAINED':'UNCONTROLLED PROPAGATION'}</p><h2>{broken===null?'EVERY HANDOFF HAS AN OWNER AND STOP POWER.':'ONE MISSING CONTROL BECOMES A DOWNSTREAM CONDITION.'}</h2><p>A workflow view shows where consequence travels. It does not calculate real-world probability or approve deployment.</p></div></section>}