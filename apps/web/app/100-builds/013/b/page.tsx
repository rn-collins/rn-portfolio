import {buildVariantMetadata} from '../../_metadata';
export const metadata=buildVariantMetadata('013','b');
import type {Metadata} from 'next';import Link from 'next/link';import RiskCircuit from './RiskCircuit';import shell from '../../001/room.module.css';export default function Build013B(){return <main className={shell.toolRoom}><nav className={shell.crumb}><Link href="/100-builds/013">← Build 013</Link><span>013-B / Web expression</span></nav><header className={shell.toolLabel}><div><span>BUILD 013-B</span><h2>Follow the Consequence</h2></div><p>A risk that enters early can survive every downstream handoff. Break one control and watch the circuit light up.</p></header><RiskCircuit/></main>}