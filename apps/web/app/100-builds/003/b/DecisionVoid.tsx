'use client';
import {useEffect,useState} from 'react';
import s from './void.module.css';
const sources=['ARTICLES','SPREADSHEETS','DASHBOARDS','EXPERTS','FORUMS','MEETINGS'];
export default function DecisionVoid(){const [density,setDensity]=useState(6);const [reveal,setReveal]=useState(false);const [auto,setAuto]=useState(true);useEffect(()=>{const m=matchMedia('(prefers-reduced-motion: reduce)');if(m.matches)setAuto(false)},[]);useEffect(()=>{if(!auto)return;const t=setInterval(()=>setReveal(v=>!v),3400);return()=>clearInterval(t)},[auto]);return <main className={s.stage}><header><span>003-B / INTERACTIVE VISUAL BUILD</span><button onClick={()=>setAuto(v=>!v)}>{auto?'PAUSE':'PLAY'}</button></header><section className={s.hero}><small>LOTS OF INFORMATION</small><h1>Still no decision.</h1><p>Move the information density. Then reveal the thing everyone is actually missing.</p></section><section className={s.field}><div className={s.cloud}>{sources.slice(0,density).map((x,i)=><button key={x} style={{'--i':i} as React.CSSProperties} onClick={()=>setReveal(true)}>{x}</button>)}</div><button className={s.void} data-reveal={reveal} onClick={()=>setReveal(v=>!v)}><span>{reveal?'THE MISSING DECISION':'?'}</span><b>{reveal?'What has to be true before we act?':'TAP THE EMPTY SPACE'}</b></button></section><section className={s.controls}><label>INFORMATION DENSITY <input aria-label="Information density" type="range" min="1" max="6" value={density} onChange={e=>setDensity(Number(e.target.value))}/></label><button onClick={()=>setReveal(v=>!v)}>{reveal?'HIDE DECISION':'REVEAL DECISION'}</button></section><footer>THE OPPORTUNITY IS OFTEN NOT MORE INFORMATION. IT IS A BETTER DECISION.</footer></main>}