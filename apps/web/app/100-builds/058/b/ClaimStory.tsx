'use client';import{useState}from'react';import{traceClaim,CLAIM_TRACE_SCENARIOS,type ScenarioId}from'../../../../../../packages/release/src/claim-trace-engine';import s from'../claim.module.css';const ids:ScenarioId[]=['FULL-RECORDED','REMOVE-PRIMARY','CONFLICTING-ROOT','MISSING-ROOT'];export default function ClaimStory(){const[i,setI]=useState(0),id=ids[i],r=traceClaim(CLAIM_TRACE_SCENARIOS[id]);return <section className={s.story}><div role="status" aria-live="polite" aria-atomic="true"><small>SUPPORT STATE {i+1}/4 · {id}</small><h2>{r.status}</h2><p>{r.nonClaims[0]}</p></div><div className={s.roots} aria-label={r.graph.length+' recorded synthetic claim-support paths'}><b>CLAIM</b>{r.graph.map(x=><span key={x}>{x}</span>)}</div><button onClick={()=>setI(v=>(v+1)%ids.length)}>REMOVE OR CHANGE A ROOT</button><p className={s.note}>Support collapse is a property of this fixed graph—not proof of falsity, causation, materiality or completeness.</p></section>}