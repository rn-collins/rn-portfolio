'use client';import{useState}from'react';import{traceContentProvenance,PROVENANCE_SCENARIOS,type ScenarioId,CONTENT_PROVENANCE_ENGINE_VERSION}from'../../../../../../packages/release/src/content-provenance-engine';import s from'../provenance.module.css';const ids:ScenarioId[]=['COMPLETE-RECORDED','REMOVE-SOURCE','REMOVE-RIGHTS','REORDER-VERSIONS'];export default function ProvenanceLab(){const[id,setId]=useState<ScenarioId>('COMPLETE-RECORDED');const input=PROVENANCE_SCENARIOS[id],r=traceContentProvenance(input);function download(){const snapshot={build:'057',version:CONTENT_PROVENANCE_ENGINE_VERSION,fixture:'synthetic',canonical:{title:'Show Me Where This Content Came From',job:'Trace a finished content object through sources, people, tools, AI involvement, rights and versions.',bWeb:'Peel the artifact backward through a synchronized provenance graph and timeline.',uses:['020','023','029'],creates:['cap:057']},input:structuredClone(input),result:structuredClone(r),replay:{instruction:'Run traceContentProvenance(input) with engine 057.1.0; perform no truth, authorship, ownership, permission, authenticity, identity, attribution, completeness, admissibility, or legal inference.',sideEffects:false,mode:'state-only'},boundary:'Fixed synthetic recorded assertions only; not copyright, title, permission, authorization, authenticity, attribution, identity, chain-of-custody, legal, or evidentiary proof.'};const u=URL.createObjectURL(new Blob([JSON.stringify(snapshot,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=u;a.download='synthetic-content-provenance.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),0)}return <section className={s.lab}><header role="status" aria-live="polite" aria-atomic="true"><small>{id}</small><h2>{r.status}</h2><p>{r.gaps.join(' · ')||'NO GAP IN THE FIXED RECORDED FIELDS'}</p></header><div className={s.controls}><label>TRACE STATE<select value={id} onChange={e=>setId(e.target.value as ScenarioId)}>{ids.map(x=><option key={x}>{x.replaceAll('-',' ')}</option>)}</select></label><button onClick={()=>setId('COMPLETE-RECORDED')}>RESTORE RECORDED TRACE</button><button onClick={download}>EXPORT PROVENANCE RECORD</button></div><div className={s.columns}><article><small>GRAPH</small>{r.graph.map(x=><p key={x}>{x}</p>)}</article><article><small>TIMELINE</small>{r.timeline.map(x=><p key={x}>{x}</p>)}</article></div><aside><b>Unknown:</b> {r.unknowns.join(' · ')}</aside><p className={s.note}><b>Boundary:</b> A recorded edge or timestamp is an assertion in a synthetic fixture. It does not prove truth, authorship, ownership, license, permission, disclosure completeness, identity, authenticity, chronology, or admissibility.</p></section>}