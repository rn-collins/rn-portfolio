'use client';import{useState}from'react';import{PRESETS,learn,type Mode}from'../../../../../../packages/release/src/cannabis-experience-engine';import s from'../experience.module.css';export default function Story(){const[open,setOpen]=useState(false),[population,setPopulation]=useState(false),[remove,setRemove]=useState(false),mode:Mode=population?'POPULATION':'INDIVIDUAL',result=learn({...PRESETS.WITHIN_PERSON,mode,removeContext:remove});return <section><header className={s.status} role="status" aria-live="polite"><small>{open?'EXPERIENCE CONSTELLATION REVEALED':'CONSTELLATION CLOSED'}</small><h2>{remove?result.disposition:population?'POPULATION SCOPE':'INDIVIDUAL SCOPE'}</h2></header><article className={s.card}><small>COMPOSITION · FORMAT · PERSON · CONTEXT · TIME · RESPONSE · TOLERABILITY</small><h3>Patterns remain scoped, uncertain and non-prescriptive.</h3><button aria-expanded={open} aria-controls="constellation-096" onClick={()=>{setOpen(v=>!v);setPopulation(false);setRemove(false)}}>{open?'HIDE EXPERIENCE CONSTELLATION':'REVEAL EXPERIENCE CONSTELLATION'}</button>{open&&<button aria-pressed={population} onClick={()=>setPopulation(v=>!v)}>COMPARE POPULATION SCOPE</button>}{open&&<button aria-pressed={remove} onClick={()=>setRemove(true)}>REMOVE CONTEXT EVIDENCE</button>}</article><ol id="constellation-096" className={s.constellation} hidden={!open}>{result.constellation.map(x=><li key={x.type} className={`${s.item} ${x.state!=='OBSERVED'?s.blocked:''}`}><small>{x.state}</small><h3>{x.type}</h3><p>Observed, missing, limited or blocked—never inferred from a label.</p></li>)}</ol><aside className={s.boundary}><b>Individual pattern ≠ population pattern ≠ personal prediction.</b><p>Experience co-occurred in frozen synthetic records. It did not “work,” prove safety or cause an outcome.</p></aside></section>}