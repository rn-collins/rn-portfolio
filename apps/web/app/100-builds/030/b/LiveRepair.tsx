'use client';
import {useState} from 'react';import s from '../inclusive.module.css';
const steps=[{label:'DEFAULT ASSUMPTIONS',className:'',body:'This synthetic fixture contains small targets, color-dependent meaning, dense language, and decorative motion.'},{label:'EXPOSE MAPPED CONSTRAINTS',className:'stress',body:'A deterministic demonstration highlights dependencies for narrow bandwidth, keyboard input, and non-color access. It does not reproduce anyone’s experience.'},{label:'ENABLE DERIVED CONTROLS',className:'repair',body:'Labels, focus indication, target size, contrast, plainer language, and lightweight delivery are present for inspection—not proven effective.'}];
export default function LiveRepair(){const [step,setStep]=useState(0);const x=steps[step];return <section className={s.story}><header><span>BUILD 030-B</span><p>THE INTERFACE HAS ASSUMPTIONS</p><h2>An interface transforms live under constraints, then the visitor repairs it.</h2></header><div className={`${s.transform} ${x.className==='stress'?s.stress:x.className==='repair'?s.repair:''}`} aria-label={`Synthetic interface view: ${x.label}`}><i>STATUS: <span>{step===2?'ACTION REQUIRED':'PRIORITY'}</span></i><b>REVIEW REQUEST</b><button tabIndex={step===2?0:-1} aria-hidden={step!==2}>ACT</button><em aria-hidden="true">DECORATIVE MOTION</em></div><article role="status" aria-live="polite" aria-atomic="true"><small>VIEW {step+1} / {steps.length}</small><h2>{x.label}</h2><p>{x.body}</p></article><div className={s.actions} aria-label="Transformation steps"><button disabled={step===0} onClick={()=>setStep(v=>Math.max(0,v-1))}>PREVIOUS VIEW</button><button onClick={()=>setStep(v=>(v+1)%steps.length)}>NEXT VIEW</button></div><p className={s.note}>A derived, deterministic interface-condition demonstration. It does not simulate disability, culture, cognition, language community, a device, network, or lived experience; it does not certify usability, localization, WCAG, or legal compliance. Manual assistive-technology, device, network, language, and participant testing remain required.</p></section>}
