'use client';import {useState} from 'react';import s from '../inclusive.module.css';const steps=[{label:'DEFAULT ASSUMPTIONS',className:'',body:'Small targets, color-only meaning, dense language and decorative motion hide exclusion.'},{label:'APPLY CONDITIONS',className:'stress',body:'Narrow bandwidth, keyboard use and non-color access expose the interface’s dependencies.'},{label:'REPAIR LIVE',className:'repair',body:'Labels, focus, target size, contrast, plain language and lightweight delivery become structural.'}];export default function LiveRepair(){const [step,setStep]=useState(0);const x=steps[step];return <section className={s.story}><header><span>BUILD 030-B</span><p>THE INTERFACE HAS ASSUMPTIONS</p><h1>An interface transforms live under constraints, then the visitor repairs it.</h1></header><div className={`${s.transform} ${x.className==='stress'?s.stress:x.className==='repair'?s.repair:''}`} aria-hidden="true"><i>STATUS</i><b>REVIEW REQUEST</b><button>ACT</button><em>MOTION</em></div><article role="status" aria-live="polite" aria-atomic="true"><small>VIEW {step+1} / {steps.length}</small><h2>{x.label}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%steps.length)}>TRANSFORM THE INTERFACE</button><p className={s.note}>Derived interface-condition demonstration. It does not reproduce disability, culture, cognition, language community, or lived experience and does not certify compliance.</p></section>}
