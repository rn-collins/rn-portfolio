'use client';import {useState} from 'react';import s from '../evaluation.module.css';const scenes=[{title:'ONE SCORE HIDES THE TASK',a:92,b:88,body:'A headline score puts Model A first.'},{title:'CONSEQUENCE CHANGES WEIGHTS',a:82,b:91,body:'Abstention and reviewability move Model B ahead.'},{title:'THE RARE SLICE DECIDES',a:74,b:83,body:'Worst-slice performance blocks the apparent winner.'}];export default function MetricSurface(){const [step,setStep]=useState(0);const x=scenes[step];return <section className={s.story}><header><span>BUILD 038-B</span><p>GOOD FOR WHICH JOB?</p><h1>A metric surface changes rankings as use case, weights and slices change.</h1></header><div className={s.surface} aria-hidden="true"><i style={{height:`${x.a}%`}}>A {x.a}</i><i style={{height:`${x.b}%`}}>B {x.b}</i></div><article role="status" aria-live="polite" aria-atomic="true"><small>VIEW {step+1} / 3</small><h2>{x.title}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%scenes.length)}>{step===2?'RESET METRICS':'CHANGE EVALUATION CONTEXT'}</button><p className={s.note}>Derived synthetic scores; not benchmark generalization, certification, or deployment authorization.</p></section>}
