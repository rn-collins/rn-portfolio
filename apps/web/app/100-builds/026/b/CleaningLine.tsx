'use client';import {useState} from'react';import s from'../messy.module.css';const states=[{label:'FIXTURE INPUT',active:0,body:'Invented names, units, values and identifiers begin in deliberately different formats.'},{label:'APPLY FIXED RULES',active:1,body:'Each synthetic field change displays its named fixture rule, before-value and after-value.'},{label:'HOLD POSSIBLE MATCHES',active:2,body:'Possible fixture matches enter a review state; the demonstration does not identify or merge real people.'},{label:'RETAIN TRANSFORMATION LOG',active:3,body:'The transformed fixture links to its prior synthetic value and the local rule that changed it.'}];export default function CleaningLine(){const[step,setStep]=useState(0);const x=states[step];return <section className={s.story}><header><span>BUILD 026-B · SYNTHETIC FIXTURE</span><p>REVERSIBLE FIELD-TRANSFORMATION REHEARSAL</p><h1>Watch invented fields change under fixed local rules.</h1></header><div className={s.line} aria-hidden="true">{['FIXTURE','TRANSFORM','REVIEW','LOG'].map((v,i)=><i key={v} className={i<=x.active?s.on:''}>{v}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>VIEW {step+1} / {states.length}</small><h2>{x.label}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%states.length)}>MOVE THROUGH THE FIXTURE</button><p className={s.note}>Observable local behavior only. A transformation log does not prove a source is true, a rule is appropriate, a match is correct, a process is reversible outside this fixture, or two records describe the same person.</p></section>}
