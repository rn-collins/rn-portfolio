'use client';import {useState} from 'react';import s from '../messy.module.css';const states=[{label:'MESSY IN',active:0,body:'Names, units, values and identifiers arrive in incompatible forms.'},{label:'APPLY NAMED RULES',active:1,body:'Every field change identifies its rule, before-value and after-value.'},{label:'HOLD POSSIBLE DUPLICATES',active:2,body:'Possible matches enter a review queue; they are not silently merged.'},{label:'RETAIN THE TRACE',active:3,body:'The clean record still points back to its source and reversible transformation history.'}];export default function CleaningLine(){const [step,setStep]=useState(0);const x=states[step];return <section className={s.story}><header><span>BUILD 026-B</span><p>REVERSIBLE PROVENANCE</p><h1>Records normalize, deduplicate and retain reversible provenance.</h1></header><div className={s.line} aria-hidden="true">{['RAW','NORMALIZE','REVIEW','TRACE'].map((v,i)=><i key={v} className={i<=x.active?s.on:''}>{v}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>VIEW {step+1} / {states.length}</small><h2>{x.label}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%states.length)}>MOVE THROUGH THE LINE</button><p className={s.note}>Derived explanatory copy. A traceable transformation does not prove that a source record is true or that two people are the same.</p></section>}
