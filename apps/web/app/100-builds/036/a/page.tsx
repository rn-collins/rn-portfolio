import type {Metadata} from 'next';import Link from 'next/link';import SurvivalLab from './SurvivalLab';import shell from '../../001/room.module.css';export const metadata:Metadata={title:'Build 036-A — Resilience Mode / Offline Survival Test',description:'Show what still works when internet, cloud or upstream services disappear.',alternates:{canonical:'/100-builds/036/a'}};export default function Build036A(){return <main className={shell.toolRoom}><nav className={shell.crumb}><Link href="/100-builds/036">← Build 036</Link><span>036-A / Functional build</span></nav><header className={shell.toolLabel}><div><span>BUILD 036-A</span><h1>Resilience Mode / Offline Survival Test</h1></div><p>Show what still works when internet, cloud or upstream services disappear.</p></header><SurvivalLab/></main>}
