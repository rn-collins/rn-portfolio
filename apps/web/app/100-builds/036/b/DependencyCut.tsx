'use client';import {useState} from 'react';import s from '../resilience.module.css';const scenes=[{title:'EVERYTHING CONNECTED',body:'Capabilities rely invisibly on internet, identity, cloud, files, and notifications.',state:0},{title:'DEPENDENCIES SEVERED',body:'Local view, draft, and export survive; sync and notification disappear.',state:1},{title:'RECOVERY QUEUED',body:'Local actions carry portable state forward until services return.',state:2}];export default function DependencyCut(){const [step,setStep]=useState(0);const scene=scenes[step];return <section className={s.story}><header><span>BUILD 036-B</span><p>WHAT REMAINS WHEN THE CLOUD LEAVES?</p><h1>The user severs dependencies and watches functions degrade and recover.</h1></header><div className={s.network} data-state={scene.state} aria-hidden="true"><i>PRODUCT</i>{['INTERNET','IDENTITY','CLOUD','FILES','NOTICE'].map(x=><b key={x}>{x}</b>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>MODE {step+1} / 3</small><h2>{scene.title}</h2><p>{scene.body}</p></article><button onClick={()=>setStep(v=>(v+1)%scenes.length)}>{step===2?'RESET NETWORK':'CHANGE NETWORK STATE'}</button><p className={s.note}>Derived synthetic dependency demonstration; not an uptime promise or disaster-recovery certification.</p></section>}
