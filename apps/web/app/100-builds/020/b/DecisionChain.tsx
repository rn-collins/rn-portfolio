'use client';import {useState} from 'react';import s from '../ledger.module.css';const stages=[['DECISION','Enter bounded recovery mode'],['ACTOR + AUTHORITY','Duty officer · delegation IC-4'],['EVIDENCE','Severity · acknowledgement · impact'],['ALTERNATIVES','Monitor · recover · shut down'],['RESULT','Stabilized · review remains open'],['CORRECTION','Append, link, never silently replace']];export default function DecisionChain(){const [n,setN]=useState(0);return <section className={s.story}><header><span>BUILD 020-B</span><p>THE DECISION IS NOT THE RECORD</p><h1>Open the decision.</h1></header><div className={s.chain} aria-live="polite">{stages.map((x,i)=><article className={i<=n?s.active:''} key={x[0]}><small>{String(i+1).padStart(2,'0')}</small><h2>{x[0]}</h2><p>{x[1]}</p></article>)}</div><footer><p>SELECTED STAGE / {stages[n][0]}</p><button onClick={()=>setN(v=>(v+1)%stages.length)}>ADVANCE THE RECORD</button></footer></section>}