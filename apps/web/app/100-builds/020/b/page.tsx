import type {Metadata} from 'next';import Link from 'next/link';import DecisionChain from './DecisionChain';import shell from '../../001/room.module.css';export const metadata:Metadata={title:'Build 020-B — Open the Decision',description:'A decision opens into a temporal provenance and evidence chain.',alternates:{canonical:'/100-builds/020/b'}};export default function Build020B(){return <main className={shell.visualRoom}><nav className={shell.crumb}><Link href="/100-builds/020">← Build 020</Link><span>020-B / Interactive visual build</span></nav><DecisionChain/></main>}