'use client';import {useState} from 'react';import s from '../rights.module.css';
const states=[{label:'ONE CHECKBOX',body:'A bundled yes hides purpose, data, recipient, retention, reuse, and withdrawal.'},{label:'PURPOSES SEPARATED',body:'Each purpose gets its own permission state, evidence, and decision history.'},{label:'WITHDRAWAL RECORDED',body:'The old state remains visible while future consent-based processing changes; deletion and other lawful bases remain separate questions.'},{label:'RIGHTS ROUTED',body:'Access, correction, deletion, retention, withdrawal, and downstream-use questions reach a named owner, status, deadline, and escalation path.'}];
export default function LivingConsent(){const [step,setStep]=useState(0);const x=states[step];return <section className={s.story}><header><span>BUILD 023-B</span><p>CONSENT THAT CAN CHANGE ITS MIND</p><h2>A checkbox becomes a living record.</h2></header><div className={s.pulse} aria-hidden="true">{states.map((v,i)=><i key={v.label} className={i<=step?s.on:''}>{String(i+1).padStart(2,'0')}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>STATE {step+1} / {states.length}</small><h2>{x.label}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%states.length)}>ADVANCE THE RIGHTS RECORD</button><p className={s.note}>Derived explanatory copy only—not a conclusion about consent validity, applicable law, lawful basis, identity, authorization, deadlines, exemptions, deletion, or whether a right applies.</p></section>}