'use client';import {useState} from 'react';import s from '../rights.module.css';
const states=[{label:'ONE CHECKBOX',body:'A bundled yes hides purpose, data, recipient, retention, reuse, and withdrawal.'},{label:'PURPOSES SEPARATED',body:'Each purpose gets its own permission state and evidence.'},{label:'WITHDRAWAL RECORDED',body:'The old state remains visible while future processing changes.'},{label:'RIGHTS ROUTED',body:'Access, correction, deletion, retention, and downstream-use questions reach a named path.'}];
export default function LivingConsent(){const [step,setStep]=useState(0);const x=states[step];return <section className={s.story}><header><span>BUILD 023-B</span><p>CONSENT THAT CAN CHANGE ITS MIND</p><h1>A checkbox becomes a living record.</h1></header><div className={s.pulse} aria-hidden="true">{states.map((v,i)=><i key={v.label} className={i<=step?s.on:''}>{String(i+1).padStart(2,'0')}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>STATE {step+1} / {states.length}</small><h2>{x.label}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%states.length)}>ADVANCE THE RIGHTS RECORD</button><p className={s.note}>This is derived explanatory copy, not a legal conclusion that consent is valid or a right applies.</p></section>}
