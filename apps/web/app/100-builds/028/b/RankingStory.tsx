'use client';import {useState} from 'react';import s from '../search.module.css';const questions=[{q:'ACCESS',order:['OPERATIONAL ACCESS EVIDENCE','ENACTMENT ≠ IMPLEMENTATION','DATA RIGHTS NOTE'],why:'Exact access terms and primary evidence lead.'},{q:'IMPLEMENTATION',order:['ENACTMENT ≠ IMPLEMENTATION','OPERATIONAL ACCESS EVIDENCE','EVIDENCE MAP'],why:'Operational concepts move the implementation analysis upward.'},{q:'PRIVACY',order:['DATA RIGHTS NOTE','EVIDENCE MAP','OPERATIONAL ACCESS EVIDENCE'],why:'Consent, retention and deletion concepts change the order.'}];export default function RankingStory(){const [active,setActive]=useState(0);const x=questions[active];return <section className={s.story}><header><span>BUILD 028-B</span><p>THE QUESTION CHANGES THE ORDER</p><h1>Results reorganize around the actual question and ranking rationale.</h1></header><div className={s.queries}>{questions.map((x,i)=><button key={x.q} aria-pressed={active===i} onClick={()=>setActive(i)}>{x.q}</button>)}</div><div className={s.stack} aria-hidden="true">{x.order.map((v,i)=><i key={v}><b>{i+1}</b>{v}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><h2>{x.q}</h2><p>{x.why}</p></article><p className={s.note}>Derived explanatory fixture. A visible relevance score is not proof that a result is true, complete, authoritative, or sufficient.</p></section>}
