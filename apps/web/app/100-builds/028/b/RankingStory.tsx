'use client';import {useState} from 'react';import s from '../search.module.css';const questions=[{q:'ACCESS',order:['OPERATIONAL ACCESS EVIDENCE','ENACTMENT ≠ IMPLEMENTATION','DATA RIGHTS NOTE'],why:'Exact tokens and disclosed corpus classifications lead in this derived fixture.'},{q:'IMPLEMENTATION',order:['ENACTMENT ≠ IMPLEMENTATION','OPERATIONAL ACCESS EVIDENCE','EVIDENCE MAP'],why:'Declared concepts and deterministic map terms move the implementation analysis upward.'},{q:'PRIVACY',order:['DATA RIGHTS NOTE','EVIDENCE MAP','OPERATIONAL ACCESS EVIDENCE'],why:'The disclosed privacy concept map changes the order; it does not understand meaning.'}];export default function RankingStory(){const [active,setActive]=useState(0);const x=questions[active];return <section className={s.story}><header><span>BUILD 028-B</span><p>THE QUESTION CHANGES THE ORDER</p><h1>Results reorganize around the actual question and ranking rationale.</h1></header><div className={s.queries}>{questions.map((x,i)=><button key={x.q} aria-pressed={active===i} onClick={()=>setActive(i)}>{x.q}</button>)}</div><ol className={s.stack} aria-label={`Illustrative ranking for ${x.q}`}>{x.order.map(v=><li key={v}>{v}</li>)}</ol><article role="status" aria-live="polite" aria-atomic="true"><h2>{x.q}</h2><p>{x.why}</p><p>Order: {x.order.join(' → ')}</p></article><p className={s.note}>Derived explanatory fixture. “Meaning” is a versioned concept map, not semantic understanding. Visible relevance is not truth, completeness, authority, or sufficiency.</p></section>}
