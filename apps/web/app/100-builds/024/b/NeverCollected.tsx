'use client';import {useState} from 'react';import s from '../privacy.module.css';
const states=[{label:'BLOATED INTAKE',count:12,body:'The form asks because it can, not because each field is necessary.'},{label:'PURPOSE TEST',count:8,body:'Fields without a bounded use leave the intake.'},{label:'RIGHTS + RETENTION',count:5,body:'Permission, deletion, derivation, and time limits narrow the remainder.'},{label:'JUSTIFIED MINIMUM',count:3,body:'The safest excluded data never becomes a breach, inference, or secondary use.'}];
export default function NeverCollected(){const [step,setStep]=useState(0);const x=states[step];return <section className={s.story}><header><span>BUILD 024-B</span><p>THE BEST DATA RISK IS DATA NEVER COLLECTED</p><h2>A bloated intake shrinks to its justified minimum.</h2></header><div className={s.meter} aria-hidden="true">{Array.from({length:12},(_,i)=><i key={i} className={i<x.count?s.on:''}/>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>PASS {step+1} / {states.length} · {x.count} FIELDS REMAIN</small><h2>{x.label}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%states.length)}>RUN THE NEXT MINIMIZATION PASS</button><p className={s.note}>Derived explanatory copy. Fewer fields do not by themselves establish legal compliance or eliminate privacy risk.</p></section>}