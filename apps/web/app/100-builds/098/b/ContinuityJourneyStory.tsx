'use client';import{useState}from'react';import{PRESETS,coordinate}from'../../../../../../packages/release/src/psychedelic-continuity-engine';import s from'../continuity.module.css';export default function Story(){const[open,setOpen]=useState(false),[withdraw,setWithdraw]=useState(false),result=coordinate({...PRESETS.CONNECTED,withdrawConsent:withdraw});return <section><header className={s.status} role="status" aria-live="polite"><small>{open?'CONTINUITY PATH REVEALED':'CONTINUITY PATH CLOSED'}</small><h2>{withdraw?result.disposition:'CONSENT-GOVERNED HANDOFFS'}</h2></header><article className={s.card}><small>EDUCATION → PREPARATION → CARE CONTEXT → INTEGRATION → REFERRAL → FOLLOW-UP</small><h3>Disconnected events become one consent-governed continuity rehearsal.</h3><button aria-expanded={open} aria-controls="path-098" onClick={()=>{setOpen(v=>!v);setWithdraw(false)}}>{open?'HIDE CONTINUITY PATH':'REVEAL CONTINUITY PATH'}</button>{open&&<button aria-pressed={withdraw} onClick={()=>setWithdraw(true)}>WITHDRAW FICTIONAL CONSENT</button>}</article><ol id="path-098" className={s.journey} hidden={!open}>{result.journey.map(x=><li key={x.stage} className={`${s.item} ${x.state==='NOT AVAILABLE'?s.blocked:''}`}><small>{x.state}</small><h3>{x.stage}</h3><p>Consent and acceptance are checked at this boundary.</p></li>)}</ol><aside className={s.boundary}><b>Record continuity is not care continuity.</b><p>A simulated handoff receipt proves only a frozen policy result—not delivery, treatment, safety or clinical follow-up.</p></aside><aside className={`${s.boundary} ${s.urgent}`}><b>Do not rely on this demo for urgent help.</b><p>Contact local emergency services or an appropriate crisis service now.</p></aside></section>}