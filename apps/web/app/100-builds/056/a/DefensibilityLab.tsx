'use client';import{useState}from'react';import{mapDefensibility,DEFENSIBILITY_SCENARIOS,type ScenarioId,DEFENSIBILITY_TOPOLOGY_ENGINE_VERSION}from'../../../../../../packages/release/src/defensibility-topology-engine';import s from'../defensibility.module.css';const ids:ScenarioId[]=['FULL-MAP','REMOVE-DATA','REMOVE-RELATIONSHIPS','REMOVE-WORKFLOW'];export default function DefensibilityLab(){const[id,setId]=useState<ScenarioId>('FULL-MAP');const input=DEFENSIBILITY_SCENARIOS[id],result=mapDefensibility(input);function download(){const snapshot={build:'056',version:DEFENSIBILITY_TOPOLOGY_ENGINE_VERSION,fixture:'synthetic',canonical:{title:'What Do You Actually Own That Others Cannot Easily Copy?',job:'Map the real defensibility of data, rights, relationships, workflows, trust, knowledge and feedback.',bWeb:'Remove supposed moat assets and watch the defensibility topology change.',uses:['011','027','029'],creates:['cap:056']},input:structuredClone(input),result:structuredClone(result),replay:{instruction:'Run mapDefensibility(input) with engine 056.1.0; perform no legal, IP, valuation, investment, competitor, or commercial-success inference; no persistence, network request, or external action.',sideEffects:false,mode:'state-only'},boundary:'Exact synthetic asset claims only; not legal ownership, IP, exclusivity, enforceability, valuation, investment advice, competitive analysis, or a prediction of durability or commercial success.'};const u=URL.createObjectURL(new Blob([JSON.stringify(snapshot,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=u;a.download='synthetic-defensibility-map.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),0)}return <section className={s.lab}><header role="status" aria-live="polite" aria-atomic="true"><small>{id} · REMOVED {result.removed.join(', ')||'NONE'}</small><h2>{result.status}</h2><p>{result.observations.join(' · ')}</p></header><div className={s.controls}><label>TOPOLOGY STATE<select value={id} onChange={e=>setId(e.target.value as ScenarioId)}>{ids.map(x=><option key={x}>{x.replaceAll('-',' ')}</option>)}</select></label><button onClick={()=>setId('FULL-MAP')}>RESTORE FULL MAP</button><button onClick={download}>EXPORT SYNTHETIC MAP</button></div><div className={s.grid}>{result.nodes.map(n=><article key={n.assetId}><small>{n.assetId}</small><h3>{n.state}</h3><p>{n.evidence}</p></article>)}</div><aside><b>Dependencies:</b> {result.links.join(' · ')||'No declared dependency in active synthetic nodes.'}<br/><b>Unknown:</b> {result.unknowns.join(' · ')}</aside><p className={s.note}><b>Boundary:</b> These are fixed synthetic declarations. The map does not establish ownership, permission, rights, IP, exclusivity, competitive advantage, value, investability, or future commercial performance.</p></section>}