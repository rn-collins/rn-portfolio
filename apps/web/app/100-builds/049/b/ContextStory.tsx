'use client';import{useState}from'react';import{profileContext,type SensoryProfile}from'../../../../../../packages/release/src/personal-context-engine';import s from'../context.module.css';const scenes:SensoryProfile[]=[{fixtureId:'SYNTHETIC-CONTEXT-01',setting:'WORK',goal:'FOCUS',light:2,sound:3,crowding:2,temperature:1,pastResponses:[{cue:'Headphones',response:'HELPED'}]},{fixtureId:'SYNTHETIC-CONTEXT-01',setting:'HOME',goal:'RECOVER',light:1,sound:1,crowding:0,temperature:1,pastResponses:[{cue:'Quiet room',response:'HELPED'}]},{fixtureId:'SYNTHETIC-CONTEXT-01',setting:'TRAVEL',goal:'CONNECT',light:2,sound:3,crowding:3,temperature:2,pastResponses:[{cue:'Crowded transfer',response:'HARDER'}]}];export default function ContextStory(){const[i,setI]=useState(0);const r=profileContext(scenes[i]);return <section className={s.story}><div role="status" aria-live="polite" aria-atomic="true"><small>SCENE {i+1} / 3 · {scenes[i].setting}</small><h2>{r.status}</h2><p>{r.supports.join(' · ')||'Observe before changing.'}</p></div><div className={s.orbit} aria-label={`Context constellation, friction ${r.friction} of 100`}><b>{r.friction}</b><span>CONTEXT<br/>IS NOT<br/>IDENTITY</span></div><button onClick={()=>setI(v=>(v+1)%scenes.length)}>CHANGE CONTEXT</button><p className={s.note}>Synthetic scenes only. No personal data is collected, retained, transmitted, inferred, or scored.</p></section>}
