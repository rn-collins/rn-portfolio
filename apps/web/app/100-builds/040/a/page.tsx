import {buildVariantMetadata} from '../../_metadata';
export const metadata=buildVariantMetadata('040','a');
import type {Metadata} from 'next';import Link from 'next/link';import StopLab from './StopLab';import shell from '../../001/room.module.css';export default function Build040A(){return <main className={shell.toolRoom}><nav className={shell.crumb}><Link href="/100-builds/040">← Build 040</Link><span>040-A / Functional build</span></nav><header className={shell.toolLabel}><div><span>BUILD 040-A</span><h1>AI Stop / Escalate Engine</h1></div><p>Test whether an AI knows when to continue, ask, abstain, fall back, escalate or stop.</p></header><StopLab/></main>}
