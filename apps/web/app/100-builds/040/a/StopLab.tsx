'use client';import {useMemo,useState} from 'react';import {decideAgentAction} from '../../../../../../packages/release/src/stop-escalate-engine';import s from '../stop.module.css';type Scenario='routine'|'missing'|'highRisk'|'toolFailure'|'noRecourse';const scenarios:Record<Scenario,{consequence:'low'|'medium'|'high';uncertainty:number;evidenceCompleteness:number;toolFailed:boolean;humanAvailable:boolean;safeFallback:boolean;timeCritical:boolean}>={routine:{consequence:'low',uncertainty:12,evidenceCompleteness:94,toolFailed:false,humanAvailable:true,safeFallback:true,timeCritical:false},missing:{consequence:'medium',uncertainty:44,evidenceCompleteness:35,toolFailed:false,humanAvailable:true,safeFallback:true,timeCritical:false},highRisk:{consequence:'high',uncertainty:58,evidenceCompleteness:72,toolFailed:false,humanAvailable:true,safeFallback:true,timeCritical:false},toolFailure:{consequence:'medium',uncertainty:30,evidenceCompleteness:88,toolFailed:true,humanAvailable:true,safeFallback:true,timeCritical:false},noRecourse:{consequence:'high',uncertainty:78,evidenceCompleteness:46,toolFailed:false,humanAvailable:false,safeFallback:false,timeCritical:true}};export default function StopLab(){const [scenario,setScenario]=useState<Scenario>('routine');const input={...scenarios[scenario],releaseRecordId:'RELEASE-032-STOP',evaluationRecordId:'EVAL-038-STOP',traceRecordId:'TRACE-039-STOP',escalationRouteId:'ROUTE-019-STOP'};const result=useMemo(()=>decideAgentAction(input),[scenario]);function download(){const record={build:'040',schema:'stop-escalate-decision-record',version:'040.1.0',fixture:'synthetic',generatedInBrowser:true,canonical:{title:'AI Stop / Escalate Engine',job:'Test whether an AI knows when to continue, ask, abstain, fall back, escalate or stop.',bWeb:'A live decision boundary reacts to uncertainty, evidence and consequence changes.',uses:['012','019','032','038','039'],creates:['cap:040']},input,result,boundary:'Derived policy fixture; not proof an AI can recognize real uncertainty, consequence, evidence sufficiency, or safe escalation.'};const href=URL.createObjectURL(new Blob([JSON.stringify(record,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=href;a.download='stop-escalate-decision-record.json';a.click();setTimeout(()=>URL.revokeObjectURL(href),0)}return <section className={s.lab}><header role="status" aria-live="polite" aria-atomic="true"><span>SCENARIO / {scenario}</span><h2>{result.action}</h2><p>{result.reasons.join(' ')}</p></header><div className={s.actions}>{(Object.keys(scenarios) as Scenario[]).map(x=><button key={x} aria-pressed={scenario===x} className={scenario===x?s.primary:''} onClick={()=>setScenario(x)}>{x.replace(/([A-Z])/g,' $1')}</button>)}<button onClick={download}>EXPORT BOUNDARY RECORD</button></div><div className={s.grid}><section><h3>Live signals</h3><dl><div><dt>Consequence</dt><dd>{input.consequence}</dd></div><div><dt>Uncertainty</dt><dd>{input.uncertainty}%</dd></div><div><dt>Evidence complete</dt><dd>{input.evidenceCompleteness}%</dd></div><div><dt>Tool failed</dt><dd>{String(input.toolFailed)}</dd></div><div><dt>Human available</dt><dd>{String(input.humanAvailable)}</dd></div><div><dt>Safe fallback</dt><dd>{String(input.safeFallback)}</dd></div></dl></section><section><h3>Decision boundary</h3>{['CONTINUE','ASK','ABSTAIN','FALLBACK','ESCALATE','STOP'].map(x=><article key={x} className={x===result.action?s.active:''}><b>{x}</b><small>{x===result.action?'Selected by deterministic rule order':'Not selected for this fixture'}</small></article>)}</section></div><p className={s.note}><b>Boundary:</b> Frozen canon is separated from derived scenarios, thresholds, priority rules, reasons, and copy. BOUNDARY ACTIVE does not establish real-world safety, recognition, or authorization.</p></section>}
