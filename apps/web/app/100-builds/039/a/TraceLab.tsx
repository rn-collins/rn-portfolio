'use client';import {useMemo,useState} from 'react';import {inspectAgentTrace,type AgentStep} from '../../../../../../packages/release/src/agent-trace-engine';import s from '../trace.module.css';const good:AgentStep[]=[{id:'S1',kind:'plan',parentId:null,tool:'',evidenceId:'',status:'success'},{id:'S2',kind:'tool',parentId:'S1',tool:'retrieve_sources',evidenceId:'',status:'success'},{id:'S3',kind:'evidence',parentId:'S2',tool:'',evidenceId:'SRC-PRIMARY-01',status:'success'},{id:'S4',kind:'decision',parentId:'S3',tool:'',evidenceId:'DECISION-020-TRACE',status:'success'},{id:'S5',kind:'answer',parentId:'S4',tool:'',evidenceId:'SRC-PRIMARY-01',status:'success'}];export default function TraceLab(){const [broken,setBroken]=useState(true);const steps=broken?good.map(s=>s.id==='S3'?{...s,evidenceId:'',status:'failed' as const}:s):good;const input={steps,releaseRecordId:'RELEASE-032-TRACE',incidentRecordId:'INCIDENT-034-TRACE',evaluationRecordId:'EVAL-038-TRACE',decisionRecordId:'DECISION-020-TRACE'};const result=useMemo(()=>inspectAgentTrace(input),[broken]);function download(){const record={build:'039',schema:'agent-work-trace-record',version:'039.1.0',fixture:'synthetic',generatedInBrowser:true,canonical:{title:'Agent Work Trace Inspector',job:'Audit whether an AI agent actually did the job well, not just produced a convincing answer.',bWeb:'Branch and replay the agent’s tool and evidence trajectory.',uses:['020','032','034','038'],creates:['cap:039']},input,result,boundary:'Derived trace fixture; not proof of model intent, truth, completeness, safety, or real agent performance.'};const href=URL.createObjectURL(new Blob([JSON.stringify(record,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=href;a.download='agent-work-trace-record.json';a.click();setTimeout(()=>URL.revokeObjectURL(href),0)}return <section className={s.lab}><header role="status" aria-live="polite" aria-atomic="true"><span>{steps.length} TRACE STEPS / SYNTHETIC</span><h2>{result.status}</h2><p>{result.failed.length} failed · {result.evidenceGaps.length} evidence gaps</p></header><div className={s.actions}><button className={s.primary} onClick={()=>setBroken(false)}>REPLAY WITH VERIFIED EVIDENCE</button><button onClick={()=>setBroken(true)}>RESTORE FAILED BRANCH</button><button onClick={download}>EXPORT TRACE RECORD</button></div><div className={s.trace}>{steps.map((x,i)=><article key={x.id} data-status={x.status}><i>{i+1}</i><div><b>{x.id} · {x.kind}</b><p>{x.tool||x.evidenceId||'No external action'}</p><small>parent {x.parentId||'ROOT'} · {x.status}</small></div></article>)}</div><p className={s.note}><b>Boundary:</b> Synthetic steps expose declared trajectory only. Frozen canon is separated from derived tools, evidence, statuses, and copy. INSPECTABLE FIXTURE does not prove correctness or quality.</p></section>}
