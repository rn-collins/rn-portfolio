import {buildVariantMetadata} from '../../_metadata';
export const metadata=buildVariantMetadata('039','b');
import type {Metadata} from 'next';import Link from 'next/link';import TraceReplay from './TraceReplay';import shell from '../../001/room.module.css';export default function Build039B(){return <main className={shell.visualRoom}><nav className={shell.crumb}><Link href="/100-builds/039">← Build 039</Link><span>039-B / Interactive visual build</span></nav><TraceReplay/></main>}
