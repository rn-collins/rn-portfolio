'use client';import {useState} from 'react';import s from '../trace.module.css';const scenes=[['CONVINCING ANSWER','The output looks complete while its evidence branch is missing.'],['BRANCH THE TRACE','Replay from retrieval and preserve the failed path.'],['EVIDENCE RECONNECTED','The answer now points through tool, source, and decision records.']];export default function TraceReplay(){const [step,setStep]=useState(0);return <section className={s.story}><header><span>BUILD 039-B</span><p>THE ANSWER IS ONLY THE LAST NODE</p><h2>Branch and replay the agent’s tool and evidence trajectory.</h2></header><div className={s.graph} data-step={step} aria-hidden="true">{['PLAN','TOOL','SOURCE','DECIDE','ANSWER'].map(x=><i key={x}>{x}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>REPLAY {step+1}/3</small><h2>{scenes[step][0]}</h2><p>{scenes[step][1]}</p></article><button onClick={()=>setStep(v=>(v+1)%3)}>{step===2?'RESET TRACE':'BRANCH AND REPLAY'}</button><p className={s.note}>Derived synthetic trace; not proof of intent, truth, completeness, or real agent performance.</p></section>}
