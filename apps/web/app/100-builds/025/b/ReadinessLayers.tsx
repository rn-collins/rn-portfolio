'use client';import {useState} from 'react';import s from '../readiness.module.css';const states=[{label:'THE PITCH',active:0,body:'The use sounds ready because the product demonstration worked.'},{label:'OPEN THE X-RAY',active:2,body:'People and workflow exist, but evidence and data are incomplete.'},{label:'FIND THE BLOCKER',active:3,body:'No named stop authority means the organization cannot control failure.'},{label:'BOUND THE PILOT',active:5,body:'Evidence, owners, limits, success measures, and incident routing make a small test defensible.'}];export default function ReadinessLayers(){const [step,setStep]=useState(0);const x=states[step];return <section className={s.story}><header><span>BUILD 025-B</span><p>READINESS HAS LAYERS</p><h1>Readiness layers reveal missing people, data, workflows, controls and evidence.</h1></header><div className={s.rings} aria-hidden="true">{['PEOPLE','DATA','WORKFLOW','CONTROLS','EVIDENCE'].map((v,i)=><i key={v} className={i<x.active?s.on:''}>{v}</i>)}</div><article role="status" aria-live="polite" aria-atomic="true"><small>VIEW {step+1} / {states.length}</small><h2>{x.label}</h2><p>{x.body}</p></article><button onClick={()=>setStep(v=>(v+1)%states.length)}>REVEAL THE NEXT LAYER</button><p className={s.note}>Derived explanatory copy. Readiness under this rubric is not authorization for production deployment.</p></section>}