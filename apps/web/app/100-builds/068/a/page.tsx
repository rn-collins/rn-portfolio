import {buildVariantMetadata} from '../../_metadata';
export const metadata=buildVariantMetadata('068','a');
import AIDigitalTwinRuntime from'./AIDigitalTwinRuntime';
import s from'../action-runtime.module.css';

export default function Page(){return <main className={s.page} data-build="068" data-variant="A"><nav aria-label="Build navigation"><a href="/100-builds">100 BUILDS</a><span>068-A</span></nav><p className={s.kicker}>SYNTHETIC · GOVERNED · NO SIDE EFFECTS</p><h1>Action begins at the approval boundary.</h1><p className={s.lede}>Inspect how a bounded twin retrieves fixed context, prepares work, recommends within limits, proposes tool use, requests approval and preserves every outcome as a separate state.</p><AIDigitalTwinRuntime/></main>}
