'use client';import{useState}from'react';import{IDEAS,PRESETS,incubate,type IdeaId}from'../../../../../../packages/release/src/idea-incubation-engine';import s from'../incubation.module.css';export default function Story(){const[id,setId]=useState<IdeaId>('QUIET'),[open,setOpen]=useState(false),result=incubate(PRESETS[id]),idea=IDEAS.find(x=>x.id===id)!;return <section><header className={s.status} role="status" aria-live="polite"><small>{open?'SYNTHETIC TEST RECORD REVEALED':'IDEA · FIVE UNANSWERED ASSUMPTIONS'}</small><h2>{open?result.decision:'AN IDEA IS NOT EVIDENCE YET'}</h2></header><div className={s.tabs}>{IDEAS.map(x=><button key={x.id} aria-pressed={id===x.id} onClick={()=>{setId(x.id);setOpen(false)}}>{x.title}</button>)}</div><article className={s.idea}><small>{idea.audience}</small><h3>{idea.title}</h3><p>{idea.idea}</p><button onClick={()=>setOpen(v=>!v)}>{open?'RESET TEST RECORD':'REVEAL TEST RECORD'}</button></article>{open&&<div className={s.pipeline}><div><b>IDEA</b><span>{idea.title}</span></div><div><b>CONTENT PROBE</b><span>{idea.experiments[0].kind}</span></div><div><b>SYNTHETIC SIGNAL</b><span>Coverage {result.coverage}%</span></div><div><b>NEXT DECISION</b><span>{result.decision}</span></div></div>}<aside className={s.boundary}><b>The post is a prototype, not proof.</b><p>Attention is not demand. Saves are not purchase intent. A simulated test can only prepare a bounded real-world test with predeclared success and stop rules.</p></aside></section>}