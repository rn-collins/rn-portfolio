'use client';import{useState}from'react';import{LINEAGE}from'../../../../../../packages/release/src/build100-scenario-engine';import s from'../scenario.module.css';export default function Convergence(){const[open,setOpen]=useState(false);const[back,setBack]=useState(false);return <section className={s.convergence}><button aria-expanded={open} aria-controls="final-system" onClick={()=>setOpen(!open)}>Enter the decision room {open?'−':'+'}</button>{open&&<div id="final-system"><div className={s.island} role="img" aria-label="Abstract island scenario system"><span>SHOCK</span><span>CONSEQUENCES</span><span>INTERVENTIONS</span><strong>DECISION</strong></div><button aria-pressed={back} onClick={()=>setBack(!back)}>{back?'Return to scenario':'Zoom backward through lineage'}</button>{back&&<div className={s.lineage}><p>17 direct capabilities · 100 builds of accumulated infrastructure</p><ol>{LINEAGE.map(id=><li key={id}><a href={'/100-builds/'+id+'/a'}>Build {id}</a></li>)}</ol></div>}<article className={s.record}><p>ACCOUNTABILITY SURVIVES THE SIMULATION</p><h2>Evidence → alternatives → tradeoffs → owner → decision record</h2></article></div>}<p className={s.note}>Synthetic system map. No prediction, emergency routing or operational action.</p></section>}