'use client';import{useState}from'react';import{LINEAGE}from'../../../../../../packages/release/src/build100-scenario-engine';import s from'../scenario.module.css';
const ALL=Array.from({length:100},(_,i)=>String(i+1).padStart(3,'0'));
export default function Convergence(){const[open,setOpen]=useState(false);const[back,setBack]=useState(false);return <section className={s.convergence}><button aria-expanded={open} aria-controls="final-system" onClick={()=>setOpen(!open)}>Enter the decision room {open?'−':'+'}</button>{open&&<div id="final-system"><div className={s.island} role="img" aria-label="Abstract island scenario system"><span>SHOCK</span><span>CONSEQUENCES</span><span>INTERVENTIONS</span><strong>DECISION</strong></div><button aria-pressed={back} onClick={()=>setBack(!back)}>{back?'Return to scenario':'Zoom backward through lineage'}</button>{back&&<div className={s.lineage}><p><b>17 direct dependencies</b> within the complete 001–100 build sequence</p><ol className={s.sequence}>{ALL.map(id=>{const direct=LINEAGE.includes(id);return <li key={id} data-direct={direct} data-current={id==='100'}><a href={'/100-builds/'+id+'/a'} aria-label={'Build '+id+(direct?', direct dependency':id==='100'?', current capstone':', sequence context')}>{id}</a></li>})}</ol><p>Direct dependencies: {LINEAGE.join(' · ')}</p></div>}<article className={s.record}><p>ACCOUNTABILITY SURVIVES THE SIMULATION</p><h2>Evidence → alternatives → tradeoffs → owner → decision record</h2></article></div>}<p className={s.note}>Synthetic system map. No prediction, emergency routing or operational action.</p></section>