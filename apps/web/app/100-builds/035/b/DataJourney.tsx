'use client';import {useState} from 'react';import s from '../portability.module.css';const scenes=[{title:'TRAPPED IN SOURCE',body:'A readable screen is not yet an export.',state:0},{title:'DATA LEAVES—MEANING BREAKS',body:'Files exist, but required fields and target mappings disappear.',state:1},{title:'THE RECORD SURVIVES',body:'Schema, provenance, required fields and target mappings travel together.',state:2}];export default function DataJourney(){const [step,setStep]=useState(0);const scene=scenes[step];return <section className={s.story}><header><span>BUILD 035-B</span><p>DOWNLOAD IS NOT PORTABILITY</p><h1>Data attempts to leave one system and survive another.</h1></header><div className={s.journey} data-state={scene.state} aria-hidden="true"><div>SOURCE</div><i>RECORD</i><b>EXPORT</b><div>TARGET</div></div><article role="status" aria-live="polite" aria-atomic="true"><small>TRANSFER {step+1} / 3</small><h2>{scene.title}</h2><p>{scene.body}</p></article><button onClick={()=>setStep(v=>(v+1)%scenes.length)}>{step===2?'RESET TRANSFER':'TEST NEXT TRANSFER'}</button><p className={s.note}>Derived synthetic transfer; not proof of legal compliance, semantic equivalence, or real-system compatibility.</p></section>}
