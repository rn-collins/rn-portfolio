'use client';import {useState} from 'react';import s from '../portability.module.css';const scenes=[{title:'SOURCE-BOUND',body:'A readable screen is not an export, and an export is not portability.',state:0},{title:'PARSEABLE, STRUCTURALLY LOSSY',body:'A file exists, but types, nulls, order, references, attachments, provenance, or rights can break reuse.',state:1},{title:'STRUCTURAL ROUND TRIP MODELED',body:'Declared synthetic values survive one tested version mapping; no real target import or data movement occurred.',state:2}];export default function DataJourney(){const [step,setStep]=useState(0),scene=scenes[step];return <section className={s.story}><header><span>BUILD 035-B</span><p>DOWNLOAD IS NOT PORTABILITY</p><h1>Data attempts to leave one system and survive another.</h1></header><div className={s.journey} data-state={scene.state} aria-label={`Synthetic data journey: ${scene.title}`}><div>SOURCE</div><i>MODELED RECORD</i><b>LOSS FINDINGS</b><div>TARGET FIXTURE</div></div><article role="status" aria-live="polite" aria-atomic="true"><small>TRANSFER MODEL {step+1} / 3</small><h2>{scene.title}</h2><p>{scene.body}</p></article><div className={s.actions}><button disabled={step===0} onClick={()=>setStep(v=>Math.max(0,v-1))}>PREVIOUS VIEW</button><button onClick={()=>setStep(v=>(v+1)%scenes.length)}>NEXT VIEW</button></div><p className={s.note}>Synthetic modeled transfer only—not real movement, deletion, legal portability, semantic equivalence, attachment validation, vendor compatibility, or successful migration.</p></section>}
