'use client';import {useState} from 'react';import s from './judgment-layers.module.css';const layers=[['RESEARCH','AI MAY ASSIST','Find candidate authority. Verify every source.'],['INTERPRETATION','LAWYER SUPERVISES','Expose alternatives, conflict, uncertainty, and adverse authority.'],['INFERENCE','LAWYER OWNS','Apply verified law to record facts and material assumptions.'],['STRATEGY','LAWYER OWNS','Weigh objectives, risk, cost, timing, and options.'],['APPROVAL','LAWYER OWNS','Sign, send, file, withhold, revise, or escalate.'],['CLIENT AUTHORITY','CLIENT DECIDES','Objectives and decisions allocated to the client remain with the client.']] as const;export default function JudgmentLayers(){const [focus,setFocus]=useState(0);return <section className={s.stage}><div className={s.stack}>{layers.map((x,i)=><button key={x[0]} onClick={()=>setFocus(i)} aria-pressed={focus===i}><span>{String(i+1).padStart(2,'0')}</span><strong>{x[0]}</strong><small>{x[1]}</small></button>)}</div><div className={s.answer}><p>{layers[focus][1]}</p><h2>{layers[focus][0]}</h2><p>{layers[focus][2]}</p><div className={s.rule}>THE TOOL MAY PRODUCE TEXT.<br/>RESPONSIBILITY DOES NOT MOVE WITH THE TEXT.</div><p>Allocation depends on the matter, jurisdiction, applicable rules, agreement, and competent supervising counsel.</p></div></section>}