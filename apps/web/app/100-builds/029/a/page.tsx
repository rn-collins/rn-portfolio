import {buildVariantMetadata} from '../../_metadata';
export const metadata=buildVariantMetadata('029','a');
import type {Metadata} from 'next';import Link from 'next/link';import ChangeLab from './ChangeLab';import shell from '../../001/room.module.css';export default function Build029A(){return <main className={shell.toolRoom}><nav className={shell.crumb}><Link href="/100-builds/029">← Build 029</Link><span>029-A / Functional build</span></nav><header className={shell.toolLabel}><div><span>BUILD 029-A</span><h1>Change &amp; Consequence Explorer</h1></div><p>Show what changed between versions and what downstream records or decisions are affected.</p></header><ChangeLab/></main>}
