'use client';import{useState}from'react';import s from'../ownership.module.css';export default function OwnershipTopology(){const[assigned,setAssigned]=useState(false);return <section className={s.story}><header><span>BUILD 041-B</span><p>ACCOUNTABILITY MUST HAVE AN ADDRESS</p><h1>An organizational topology reveals orphaned accountability and changes under reassignment.</h1></header><div className={s.topology} data-assigned={assigned} aria-hidden="true"><i>TOOL</i><i>VENDOR</i><i>DECISION</i><i>WORKFLOW</i><b>OWNER</b></div><article role="status" aria-live="polite"><small>TOPOLOGY STATE</small><h2>{assigned?'REASSIGNED':'ORPHANED'}</h2><p>{assigned?'Accountable owner and decision authority now connect to the workflow.':'The workflow is operated but has no declared accountable owner or decision authority.'}</p></article><button onClick={()=>setAssigned(v=>!v)}>{assigned?'REMOVE ASSIGNMENT':'REASSIGN OWNERSHIP'}</button><p className={s.note}>Synthetic declared topology; not a legal conclusion or proof of operational control.</p></section>}
