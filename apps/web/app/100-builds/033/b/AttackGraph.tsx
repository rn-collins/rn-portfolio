'use client';import {useState} from 'react';import s from '../adversarial.module.css';const scenes=[{goal:'MANIPULATE',control:'NO CONTROL',status:'PATH OPEN',body:'A crafted input reaches the consequential decision.'},{goal:'MANIPULATE',control:'VALIDATE + REVIEW',status:'PATH INTERRUPTED',body:'Validation and accountable review break the mapped route.'},{goal:'EXTRACT',control:'DATA BOUNDARY',status:'GRAPH RECONFIGURED',body:'A different goal reveals a different path and control boundary.'}];export default function AttackGraph(){const [step,setStep]=useState(0);const scene=scenes[step];return <section className={s.story}><header><span>BUILD 033-B</span><p>ATTACKERS CHANGE THE QUESTION</p><h1>An attack graph changes as attacker goals and controls change.</h1></header><div className={s.graph} data-step={step} aria-hidden="true"><i>GOAL</i><i>INPUT</i><i>TOOL</i><i>DECISION</i><b>{scene.control}</b></div><article role="status" aria-live="polite" aria-atomic="true"><small>{scene.goal} / VIEW {step+1} OF 3</small><h2>{scene.status}</h2><p>{scene.body}</p></article><button onClick={()=>setStep(v=>(v+1)%scenes.length)}>{step===2?'RESET GRAPH':'CHANGE GOAL / CONTROL'}</button><p className={s.note}>Derived high-level defensive demonstration; not exploit instructions, penetration testing, or security certification.</p></section>}
