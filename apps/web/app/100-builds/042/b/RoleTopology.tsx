'use client';import{useState}from'react';import s from'../role.module.css';export default function RoleTopology(){const[step,setStep]=useState(0);const states=[['READ','Bounded retrieval · human synthesis'],['DRAFT','Draft responsibility added · evaluation expands'],['WRITE BLOCKED','Write added without before-write approval'],['WRITE REVIEWED','Human review point restores the declared gate']];return <section className={s.story}><header><span>BUILD 042-B</span><p>A JOB IS A CONTROL SURFACE</p><h1>Drag responsibilities in and out and watch permissions, risk and evaluation recompute.</h1></header><div className={s.topology} data-step={step} aria-hidden="true"><i>ROLE</i><i>TOOLS</i><i>REVIEW</i><i>STOP</i><b/></div><article role="status" aria-live="polite"><small>ROLE STATE {step+1}/4</small><h2>{states[step][0]}</h2><p>{states[step][1]}</p></article><button onClick={()=>setStep(v=>(v+1)%4)}>{step===3?'RESET ROLE':'MOVE RESPONSIBILITY'}</button><p className={s.note}>Synthetic declared role topology; not permission for real execution.</p></section>}
