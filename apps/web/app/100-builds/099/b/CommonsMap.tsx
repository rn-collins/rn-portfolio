'use client';import{useState}from'react';import{DATASETS}from'../../../../../../packages/release/src/build099-commons-engine';import s from'../commons.module.css';export default function CommonsMap(){const[open,setOpen]=useState(false);const[selected,setSelected]=useState(0);const d=DATASETS[selected];return <section className={s.visual}><div className={s.map} role="img" aria-label="Abstract fictional island data commons"><button className={s.node1} aria-pressed={selected===0} onClick={()=>setSelected(0)}>{DATASETS[0].island}</button><button className={s.node2} aria-pressed={selected===1} onClick={()=>setSelected(1)}>{DATASETS[1].island}</button><button className={s.node3} aria-pressed={selected===2} onClick={()=>setSelected(2)}>{DATASETS[2].island}</button><span className={s.ring}/></div><button aria-expanded={open} aria-controls="dataset-card" onClick={()=>setOpen(!open)}>Synthetic dataset passport {open?'−':'+'}</button>{open&&<article id="dataset-card" className={s.passport}><p>{d.island} · {d.access}</p><h2>{d.name}</h2><dl><div><dt>FICTIONAL STEWARD</dt><dd>{d.steward}</dd></div><div><dt>FIXTURE DATE</dt><dd>{d.updated}</dd></div><div><dt>SYNTHETIC SOURCE</dt><dd>{d.source}</dd></div></dl><p>In this frozen model, metadata remains attached as the fixture moves between simulated states.</p></article>}<p className={s.note}>Entirely fictional topology, organizations, dates, records and permissions. No real island, community, authority, consent, ownership or sovereignty is represented.</p></section>}