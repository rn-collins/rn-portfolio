'use client';import{useState}from'react';import{DATASETS}from'../../../../../../packages/release/src/build099-commons-engine';import s from'../commons.module.css';export default function CommonsMap(){const[open,setOpen]=useState(false);const[selected,setSelected]=useState(0);const d=DATASETS[selected];return <section className={s.visual}><div className={s.map} role="img" aria-label="Abstract island data commons"><button className={s.node1} aria-pressed={selected===0} onClick={()=>setSelected(0)}>Hawaiʻi</button><button className={s.node2} aria-pressed={selected===1} onClick={()=>setSelected(1)}>Maui</button><button className={s.node3} aria-pressed={selected===2} onClick={()=>setSelected(2)}>Molokaʻi</button><span className={s.ring}/></div><button aria-expanded={open} aria-controls="dataset-card" onClick={()=>setOpen(!open)}>Dataset passport {open?'−':'+'}</button>{open&&<article id="dataset-card" className={s.passport}><p>{d.island} · {d.access}</p><h2>{d.name}</h2><dl><div><dt>STEWARD</dt><dd>{d.steward}</dd></div><div><dt>FRESHNESS</dt><dd>{d.updated}</dd></div><div><dt>PROVENANCE</dt><dd>{d.source}</dd></div></dl><p>Metadata stays attached as knowledge moves through the commons.</p></article>}<p className={s.note}>Illustrative topology. Locations are abstract and data are synthetic.</p></section>}